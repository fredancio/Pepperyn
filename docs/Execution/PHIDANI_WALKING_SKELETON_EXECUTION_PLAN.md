# PHIDANI_WALKING_SKELETON_EXECUTION_PLAN.md

**Nature :** Phase 10. Rend concret `PHIDANI_WALKING_SKELETON.md` contre les fichiers réels du dépôt. Aucun code écrit. À exécuter seulement une fois tous les Gates (`PRE_IMPLEMENTATION_GATE_CHECKLIST.md`) passés.

---

| Étape | Fichiers (nouveaux/modifiés) | Contrats | Dépendances | Tests | Persisté | Dérivé | Critère de Done | Rollback | Ancien chemin remplacé | Ancien chemin conservé |
|---|---|---|---|---|---|---|---|---|---|---|
| **1. Ingestion** | Aucun — `FileConnector` existant réutilisé tel quel | — | Aucune | Existants, inchangés | — | — | Le fichier Phidani (deux versions) est lu sans erreur | N/A — pas de modification | Aucun | `FileConnector` intégralement |
| **2. Normalisation → Evidence** | Nouveau : `backend/domain/evidence/fact_builder.py` (construit `FinancialFact` avec provenance) ; modifié : `backend/services/file_parser.py` (appelle le nouveau builder plutôt que de renvoyer une structure brute) | `EvidenceContext` | `temporal_normalizer.py` (déjà confirmé fournisseur, `TEMPORAL_NORMALIZER_VS_FTE_REVIEW.md`), Evidence Ledger (T1, doit être mergé au préalable — Gate C) | Test : chaque `FinancialFact` produit référence une cellule/feuille source réelle du dataset Phidani | `FinancialFact[]` dans Evidence Ledger (table `evidence_ledger`, migration `v18` déjà écrite T1C-A) | — | Tous les faits Phidani pertinents sont extraits avec provenance | Feature flag `USE_COGNITIVE_PIPELINE=false` désactive le nouveau chemin, `file_parser.py` revient au comportement actuel | Aucun encore — coexistence obligatoire tant que le nouveau chemin n'est pas validé sur Golden Case | Ancien retour brut de `file_parser.py`, utilisé par le pipeline actuel non modifié |
| **3. Contexte temporel minimal** | Nouveau : `backend/domain/temporal/fte_minimal.py` — implémente uniquement `PeriodObservation`/`FiscalPeriod` (déjà le périmètre limité recommandé par `FOUNDATION_RECOVERY_REVIEW.md` Q7) | `TemporalContext` | `temporal_normalizer.py` (fournisseur), Engagement (T2A, doit être mergé — Gate C) | Test : sur le dataset Phidani, septembre est identifié `CURRENT_ACTUAL`, août `HISTORICAL_ACTUAL`, cohérent avec `GOLDEN_CASE_001_PHIDANI.md` | Aucun — FTE sans état par conception | `BusinessMoment`, `DataFreshness`, `ComparisonHorizons` — entièrement dérivés à la demande | Le FTE minimal répond correctement aux « Must Detect » du Golden Case | Feature flag, idem étape 2 | Aucun chemin temporel existant à remplacer — n'existait pas | — |
| **4. Dossier cognitif** | Nouveau : `backend/domain/reasoning/case_file_assembler.py` (Context Assembly Engine minimal) | `CognitiveCaseFile` | Étapes 2 et 3 | Test : le dossier assemblé contient une provenance pour chaque fait, `OpenExceptions` vide accepté | Non persisté — assemblé à la demande | Entièrement dérivé de 2 et 3 | Le dossier passe la validation de schéma (`ConfidenceContract` respecté) | Feature flag | Aucun | — |
| **5. Deux analyses indépendantes** | Nouveau : `backend/services/reasoning/analyst_a.py`, `analyst_b.py` — **réutilisent** `FINANCIAL_ANALYST_SYSTEM`/`STRATEGIC_CFO_SYSTEM` existants comme point de départ des prompts (voir `REASONING_PIPELINE_MIGRATION_PLAN.md`), recâblés en appels parallèles indépendants | `IndependentAnalysis` × 2 | Étape 4 | Test : aucune trace de l'un dans les logs d'appel de l'autre (isolation vérifiable) | Non persisté directement — passe par Decision Memory à l'étape 10 | Dérivé du `CognitiveCaseFile` | Deux analyses produites, chacune avec statuts de confiance par affirmation | Feature flag ; en cas d'échec, fallback vers `run_full_pipeline` actuel (coexistence, pas de suppression) | `_run_financial_analyst_prep`/`_run_strategic_cfo_prep` (séquentiels) — remplacés fonctionnellement, pas supprimés du code tant que le nouveau chemin n'est pas validé | Anciennes fonctions conservées, non appelées par le nouveau chemin |
| **6. Adjudication** | Nouveau : `backend/services/reasoning/adjudicator.py` | `AdjudicationResult` | Étape 5 | Test Golden Case : sur Phidani, si les deux analystes divergent sur un point factuel, l'Adjudicator le signale, ne moyenne jamais | Non persisté directement | Dérivé des deux `IndependentAnalysis` | Convergences/divergences qualifiées produites, aucun consensus forcé détecté par test | Feature flag | `call_verification_v3` (Call 2 actuel) — remplacé fonctionnellement | Conservé, non appelé par le nouveau chemin |
| **7. Synthèse (Executive CFO)** | Nouveau : `backend/services/reasoning/executive_cfo.py`, entrée strictement bornée à `AdjudicationResult` (garde-fou explicite du contrat, `MULTI_AGENT_REASONING_ARCHITECTURE_PROPOSAL.md`) | `ExecutiveRecommendation` | Étape 6 | Test : aucune divergence non résolue masquée dans la sortie | Non persisté directement | Dérivé de l'`AdjudicationResult` uniquement | Recommandation produite, traçable jusqu'à l'`AdjudicationResult` | Feature flag | `call_analysis_v3` (Call 1 actuel, partie synthèse) — remplacé fonctionnellement | Conservé |
| **8. Quality Gate** | Nouveau : `backend/domain/quality/deterministic_gate.py` — **remplace** `_score_analysis` | déterministe, pas de nouveau contrat nommé (validation de contrats existants) | Étape 7 + `EvidenceContext` | Test : chaque chiffre cité est traçable ; échec injecté volontairement (chiffre sans preuve) doit être rejeté | Résultat Pass/Fail, non persisté durablement (log d'audit seulement) | Dérivé | 100 % des chiffres de la sortie Golden Case sont traçables | Feature flag ; si échec du Gate déterministe en production, fallback vers `_score_analysis` documenté comme dégradé, jamais silencieux | `_score_analysis` — **RETIRE une fois le Gate déterministe validé sur Golden Case**, pas avant | Conservé jusqu'à validation complète |
| **9. Décision humaine** | Aucun nouveau fichier — interface existante (Article II, déjà respecté par le produit) | — | Étape 8 | Existants | Décision horodatée, table existante | — | La décision est capturée | N/A | Aucun | Interface existante intégralement |
| **10. Mémoire minimale** | Nouveau : `backend/services/reasoning/learning_recorder.py` | `LearningProposal` | Étape 9, Decision Memory (existant) | Test : la prochaine exécution sur Phidani peut lire cette entrée | Entrée dans `decision_memory` (table existante) | — | L'entrée est lisible par un cycle suivant | Feature flag | Aucun — Decision Memory existant étendu, pas remplacé | Intégralement conservé |

---

## Principe de coexistence, explicite

**Aucune étape ne supprime son ancien équivalent avant que le nouveau chemin ait passé le Golden Case Phidani en replay.** Le feature flag proposé (`USE_COGNITIVE_PIPELINE`) suit exactement le précédent déjà présent dans le code réel (`USE_ENHANCED_PIPELINE`, confirmé dans `llm_service.py`) — pas un mécanisme nouveau, une extension du même pattern déjà éprouvé par l'équipe.

---

**PHIDANI_WALKING_SKELETON_EXECUTION_PLAN ÉTABLI. AUCUN CODE ÉCRIT. À EXÉCUTER APRÈS PASSAGE DE TOUS LES GATES.**
