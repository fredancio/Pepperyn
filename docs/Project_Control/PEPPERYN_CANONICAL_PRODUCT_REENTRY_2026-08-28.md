# PEPPERYN — CANONICAL PRODUCT RE-ENTRY

**Date:** 2026-08-28

**Canonical source inspected:** `main@9b17e0f3bebc33045f2197979c592368c3f39abc`

**Mission type:** Discovery + arbitration; no production implementation

**Decision status:** Product re-entry established; one next mission selected below

## 1. Executive verdict

Pepperyn on canonical `main` is a real, unusually broad financial-analysis product, not merely a collection of recovered architecture. It has authentication, organization/entity workspaces, file ingestion, an LLM analysis pipeline, executive results, PDF/PPTX/XLSX exports, analysis history and chat, billing controls, portfolio/review surfaces, decision memory, and a founder control surface. It also contains governed cognitive kernels for evidence, time, observation structure, economic meaning, doctrine, knowledge, uncertainty, admission/reconciliation, composition and passive OOXML reading.

Those two halves are not yet a single demonstrably field-safe professional workflow. Several governed kernels are canonical and tested but remain only partially connected to the product path. More urgently, the preferred Conversation Engine V2 sends a re-identified `ExecutiveCase V2` to Anthropic, while the public product promises anonymisation before AI processing. The main analysis path also has documented clear-text `relation_section`, `memory_section` and `actions_section` egress. This is a current trust defect, not a hypothetical future concern.

Therefore the shortest defensible path is:

1. close and prove the LLM-egress trust boundary for the exact first-client workflow;
2. replay one complete, isolated Founder engagement from onboarding to decision-ready output;
3. let observed professional friction select the next integration/product increment.

The next mission selected is **FIRST REAL-CLIENT LLM EGRESS TRUST GATE**. Portfolio Attention remains a validated user-test hypothesis and is not promoted to the roadmap.

## 2. Source and authority classification

The classification applies to propositions, not blindly to whole files. A foundational document may contain falsifiable mechanisms.

| Source | Classification | Operative reading |
|---|---|---|
| Pepperyn North Star | **A — Foundational/canonical intent** | The ambition and professional standard are durable; proposed architecture remains falsifiable. |
| V1 Marketability Gate | **A — Foundational product/business governance** | Finite stopping condition for a marketable V1; architecture alone cannot pass a gate. |
| Agentic Development Operating System | **B — Current operating direction** | Governs how work is selected and reviewed; does not make product hypotheses canonical. |
| GPT Work Architecture Handover | **D — Historical handover/context** | Useful lineage and warnings; repository reality and newer Founder decisions prevail. |
| Differentiation Doctrine | **A — Foundational strategy/doctrine** | Pepperyn-specific value must compound evidence, continuity, doctrine, memory and workflow. |
| Professional Empathy / User Value / Cognitive Intent | **A — Foundational product intent** | Professional usefulness, honesty and cognitive load reduction are durable; exact UX is testable. |
| Founder-as-first-Fractional-CFO Launch Strategy | **B — Current strategic direction** | Small real portfolio and field evidence are the current launch route; not a frozen roadmap. |
| Current Plan Architecture | **C — Working hypothesis** | Free/Trial → Plus → Pro → Business/Enterprise names and boundaries remain falsifiable. |
| Free Experience & Conversion Economics | **A/C** | Bounded-cost proof of differentiated value is doctrine; the exact free mechanism is a hypothesis. |
| Enterprise Onboarding & Knowledge Bootstrap | **A/C** | Trustworthy progressive familiarisation is foundational; the exact sequence and UI require field validation. |
| Cognitive Capital / Retention / Client Continuity | **A/C** | Enterprise-owned continuity is doctrine; retention effects and storage mechanisms are hypotheses. |
| Living Client Cards / Continuous Interaction | **A/C** | Continuous client understanding is durable product intent; a specific card topology is not canonical. |
| Executive Deliverables & Decision Interface | **A/C** | Decision-ready professional output is foundational; precise PDF/PPT/workspace forms are falsifiable. |
| Internal Agentic Cognitive Architecture | **A/C** | Required professional faculties and governed truth boundary are durable; agent topology/model/orchestration are hypotheses. |
| Scoring / Confidence / Epistemic Status | **A — Foundational cognitive doctrine** | Preserve kinds of uncertainty; no undefined confidence number. Capability-specific combination rules remain versioned contracts. |
| Responsive Product / Multi-Form-Factor UX | **A — Canonical founding product doctrine** | Responsive usefulness is a normal quality gate, not later polish. |
| Design & Conversion Doctrine | **A/C** | Premium restraint, usability and economic discipline are doctrine; screens, prices and funnel mechanics remain testable. |
| White-Label Identity | **F — Proposed/high-value direction** | Professional use case is credible; scope, packaging and timing require evidence. |
| Founder Administration / Control Center | **A — Foundational access-control requirement** | Platform authority must be exclusive and backend-enforced; exact mechanism is falsifiable. Repository default email currently differs from the supplied Founder identity, requiring later configuration/arbitration before production reliance. |
| Future Private Deployment | **E — Future option** | Preserve optionality without building self-hosting for V1. |
| Agentic Golden Case #1 | **C/F — Validated product/architecture hypothesis** | Honest ties/abstention accepted. Precedence and materiality ownership are not doctrine; no implementation authority. |
| Canonical repository and Product Board | **A for implemented-source reality** | Code/tests determine what exists. Product Board is the current control surface but contains dated baseline statements and must not substitute for runtime validation. |

### Recorded tensions, not silently reconciled

- The public security copy states that anonymisation occurs before AI processing; current preferred chat and parts of the main analysis pipeline violate that proposition. Code wins for present behavior; the promise remains the required standard.
- The supplied Founder Administration document names `fredanciaux@hotmail.com`; `backend/routers/superadmin.py` defaults `SUPER_ADMIN_EMAIL` to `fredanciaux16@gmail.com`. Environment configuration may override it, so effective production authority is **UNKNOWN** without runtime evidence.
- The plan ladder in product copy/code uses current labels such as `free`, `pro`, `power`, `scale`, while the recovered strategy uses Free/Trial, Plus, Pro, Business/Enterprise. These answer packaging questions still classified as hypotheses; no pricing redesign is authorized here.

## 3. Current Product State Map

### 3.1 Implemented and canonical

- Authentication, company/entity/workspace foundation, plan enforcement, billing endpoints and account lifecycle.
- File analysis path with financial parsing, LLM analysis/verification, result persistence, history and follow-up chat.
- Executive result UI and PDF, PowerPoint and Excel exports.
- Portfolio Home and Review Briefing; decision arcs, consequences, learning and decision memory.
- Evidence Ledger capture plus a deliberately limited Review Briefing consumer.
- Engagement foundation and entity continuity anchor.
- Deterministic FTE v0 kernel, Observation Structure v0, Candidate/epistemic value model, Canonical Financial Doctrine and Concept Vocabulary, PERSONNEL_COST classifier, admission/reconciliation, composition, doctrine authority and passive OOXML evidence/reader foundations represented on canonical main.
- Founder Control Center routes with backend authorization check, subject to the identity/configuration uncertainty above.

### 3.2 Implemented but needing product, UX or integration validation

- The primary upload → analysis → executive output workflow: substantial and user-facing, but not demonstrated in this mission against a complete real Founder engagement on the current production-like stack.
- Portfolio Home/Review Briefing: passed synthetic product validation with reservations; not proven to reduce work in a real small portfolio.
- Analysis chat and continuity: productized, but presently blocked for real-client use by the trust defect.
- Executive exports: real and useful-looking; professional decision utility and edit burden remain unmeasured in the field.
- Responsive and premium UX: meaningful frontend exists, but no current cross-form-factor professional validation proves the doctrine is met.
- Subscription, usage and acquisition paths: implemented surfaces exist; conversion and >=80% gross-margin feasibility are unproven.

### 3.3 Architecturally validated but not fully productized

- Governed evidence/provenance beyond analysis-level context.
- Deterministic temporal, observation, semantic, doctrine and epistemic contracts as a coherent production reasoning path.
- Knowledge Model v0 beyond its narrow confirmed subject.
- Epistemic Dialogue contract and trustworthy clarification loop.
- Actual passive OOXML semantic reading and authority lineage as a visible professional capability.
- Candidate → Doctrine → Composition foundations and trusted-current doctrine authority orchestration.

### 3.4 Documented/designed but not implemented as complete product capabilities

- Progressive enterprise familiarisation and broad enterprise knowledge bootstrap.
- Exception and reconciliation workflow.
- Full recommendation engine governed by the newer cognitive foundation.
- Living Client Cards as a persistent professional workspace.
- Collaboration/invitations and mature customer-side RBAC.
- White-label professional delivery.
- General Trust Gateway / single enforceable LLM-egress boundary.

### 3.5 Hypotheses requiring user or market evidence

- Portfolio Attention ordering and its value relative to single-client depth.
- Engagement-specific materiality ownership and override policy.
- Plan names, packaging, prices and precise free-to-paid funnel.
- Multi-agent topology and whether it outperforms a simpler strong-model workflow.
- Specific Living Client Card, onboarding and executive-deliverable forms.
- Retention and willingness-to-pay effects of cognitive continuity.

### 3.6 Deferred/future options

- Private/self-hosted deployment.
- Broad ERP/API/MCP connectors before a field case demands one.
- Portfolio benchmarking, advanced white label and enterprise deployment features.
- Broad redesign of the working application.

### 3.7 Known risks/blockers/trust gaps

- **Blocking:** preferred Conversation Engine V2 sends the re-identified case dossier to Anthropic on every turn.
- **Blocking for a trustworthy full analysis claim:** clear-text relationship, memory and action sections are documented in the main LLM analysis/verification payloads.
- Correspondence tables are held in process memory and protection can silently disappear after restart/multi-instance routing.
- LLM calls are not structurally forced through one auditable egress boundary; current closure-plan inventory is dated and must be re-verified before changing code.
- Public anonymisation language is stronger than actual coverage.
- Current production deployment configuration, cost per complete engagement, and effective Founder super-admin identity are not evidenced here.
- FTE honestly returns `UNKNOWN` for the historical Phidani `YYYY-MM` header gap; temporal truth also has a legacy `is_current_period` path not reconciled with FTE.

### 3.8 Obsolete/superseded where evidenced

- The stale GitHub-main recovery state and the separate reconstruction lineage are not current product authority.
- Archived roadmaps/Product Boards and superseded ADR versions are context, not current execution authority.
- A guaranteed single #1 client and the Golden Case precedence as canonical doctrine are explicitly rejected.

## 4. V1 Marketability Gate assessment

| Gate | Verdict | Evidence-based reason |
|---|---|---|
| Clear professional promise | **PARTIAL** | Product and outputs are tangible, but the promise spans more governed continuity than the current integrated workflow proves. |
| Trustworthy financial understanding | **PARTIAL** | Strong deterministic and epistemic foundations exist; much remains incompletely wired into the visible analysis path. |
| Professional honesty / UNKNOWN | **PARTIAL** | Canonical kernels model abstention/unknown explicitly; the legacy/product analysis path is not proven to preserve it end-to-end. |
| One coherent end-to-end workflow | **PARTIAL** | Upload, analysis, chat, history and exports exist; no current real-engagement replay proves professional completeness and chat is trust-blocked. |
| Usable executive output | **PARTIAL** | PDF/PPTX/XLSX and executive UI exist; field usefulness and manual edit burden are unknown. |
| Attention reduction | **UNKNOWN / NOT YET EVIDENCED** | Portfolio surfaces and Golden Case hypothesis exist, but no real portfolio evidence. Not required before the first single-client field engagement. |
| Persistence and continuity | **PARTIAL** | Analyses, decisions, evidence and engagement foundations persist; enterprise understanding and continuous workflow are narrow/incomplete. |
| Fractional-CFO workflow | **PARTIAL** | Multi-entity and portfolio foundations exist; a complete Founder operating loop has not been observed. |
| Premium, low-friction UX | **UNKNOWN / NOT YET EVIDENCED** | Implemented experience exists; doctrine-level quality is not currently validated. |
| Responsive experience | **UNKNOWN / NOT YET EVIDENCED** | Responsive intent and frontend implementation exist; no current device-class validation was necessary for this arbitration. |
| Security and entity isolation | **FAIL** | Known LLM-egress violations contradict the public trust promise. Entity-scoped APIs exist, but that does not cure provider egress. |
| Usable without architecture knowledge | **PARTIAL** | User-facing workflow is conventional; unresolved states and manual workarounds on a real case are unmeasured. |
| Differentiation from general-purpose GPT | **PARTIAL** | Pepperyn-specific governed assets are substantial; their combined user advantage is not yet consistently visible in one proven workflow. |
| Economic feasibility / >=80% gross margin | **UNKNOWN / NOT YET EVIDENCED** | Usage limits exist; complete engagement cost and support/edit burden are not measured. |
| Conversion/market validation | **UNKNOWN / NOT YET EVIDENCED** | Acquisition and billing surfaces do not constitute market evidence. |

**Overall V1 verdict: NOT YET MARKETABILITY-GATE COMPLETE.** The product is close enough to justify a real field-use sequence, but not safe enough to start that sequence with confidential client data until the egress trust gate is closed.

## 5. Smallest critical path to Founder field use

1. **First-real-client LLM egress trust gate** — close all proven clear-text egress in the exact upload/analysis/chat workflow; fail closed when protection authority is absent; align user-facing claims with proved behavior.
2. **One isolated end-to-end Founder engagement rehearsal** — representative real or irreversibly anonymised case; onboarding/context, ingestion, analysis, uncertainty, executive output, decision/action capture and follow-up; record time, manual escapes, unsupported assertions, cost and trust events.
3. **One evidence-selected corrective increment** — fix only the highest-severity observed obstacle to professional use.
4. **Begin a small real portfolio only after the single-engagement gate passes**; then test whether Portfolio Attention creates incremental value.

Enterprise self-hosting, broad connectors, white label and a complete agent topology are not on this shortest path.

## 6. Golden Case #1 disposition

Golden Case #1 is **accepted as a validated product/architecture hypothesis only**. It demonstrated a valuable question, a disciplined way to expose ties/abstention, and the ability of the work organization to falsify a concept. It did not prove a market need, safe materiality ownership, canonical precedence or production readiness.

Portfolio Attention is therefore a **useful but non-blocking portfolio capability**. It should wait until one real single-client workflow is safe and until a small real portfolio can test whether cross-client ordering saves more Founder attention than it costs. The proposed precedence is retained solely as a user-test hypothesis.

## 7. Candidate next missions

### Candidate 1 — First Real-Client LLM Egress Trust Gate

- **Type:** controlled security/architecture corrective mission, with current-state contract gate followed by minimal implementation and independent negative review.
- **Problem retired:** identified client data can leave the intended anonymisation boundary during normal analysis/chat.
- **Why now:** it is the only proven defect that makes tomorrow's real-client use professionally unsafe.
- **User/professional value:** confidentiality promise becomes enforceable rather than aspirational.
- **Architecture value:** one inspectable egress authority, fail-closed behavior and regression checks replace distributed discretion.
- **Business/validation value:** unlocks ethical field testing and removes a credibility/liability gap.
- **Dependencies:** none; use existing anonymisation behavior and current call sites as evidence. Re-inventory active egress before implementation.
- **Cost/complexity:** medium, bounded to first-client active paths; avoid a platform-wide redesign.
- **Principal risk:** broadening into a speculative universal gateway or silently changing analysis semantics.
- **Falsifier:** proof that active payload capture contains no re-identifiable client material and that protection cannot disappear when correspondence state is absent. Current code and audits already contradict this.
- **Implementation authority:** contract/current-path review first; minimal corrective implementation only after exact scope and tests are approved within that mission.

### Candidate 2 — Founder First-Engagement End-to-End Field Rehearsal

- **Type:** controlled product/operations validation, initially no broad development.
- **Problem retired:** uncertainty about whether the existing product can serve as the Founder's primary environment for one engagement.
- **Why now:** it produces the highest product information value once client-data egress is safe.
- **Value:** measures professional completeness, manual workarounds, decision utility, UX friction, cost and unsupported confidence.
- **Dependencies:** Candidate 1; production-like configuration and a Founder-approved case.
- **Cost/complexity:** low-to-medium.
- **Principal risk:** turning observed discomfort into an unbounded redesign.
- **Falsifier:** a complete rehearsal already exists on canonical main with measured results and no material gaps; no such evidence was found.
- **Implementation authority:** no feature implementation initially; only after the rehearsal identifies one blocker.

### Candidate 3 — Governed Analysis Path Integration Slice

- **Type:** architecture/product integration contract plus one narrow vertical implementation.
- **Problem retired:** canonical evidence/epistemic/doctrine kernels are not consistently visible in user-facing reasoning.
- **Why now:** it would make differentiation tangible and reduce generic-GPT equivalence.
- **Value:** explicit provenance, contradiction/UNKNOWN and doctrine behavior in one professional conclusion.
- **Dependencies:** select an observed high-value conclusion from Candidate 2; preserve trust gate.
- **Cost/complexity:** medium-to-high.
- **Principal risk:** integrating elegant foundations where the user does not need them most.
- **Falsifier:** field evidence shows another obstacle dominates or users obtain the same value without the integration.
- **Implementation authority:** contract/review before code.

### Candidate 4 — Portfolio Attention Real-Portfolio User Test

- **Type:** product discovery/prototype test; no production implementation initially.
- **Problem retired:** whether cross-client prioritisation materially reduces Founder attention cost.
- **Why now:** Golden Case #1 created a falsifiable hypothesis, but it lacks real evidence.
- **Value:** could become a strong Pro/fractional-CFO differentiator.
- **Dependencies:** safe field use and a small real portfolio; unresolved materiality policy must remain exposed.
- **Cost/complexity:** low for a controlled test, high if prematurely productized.
- **Principal risk:** recent-work bias and false ordering authority.
- **Falsifier:** Founder naturally works client-by-client, cannot safely rank, or sees no time/decision-quality gain.
- **Implementation authority:** user test only; separate arbitration for any implementation.

## 8. Comparative ranking

| Criterion | 1. Egress trust | 2. Field rehearsal | 3. Governed integration | 4. Portfolio test |
|---|---|---|---|---|
| Proximity to safe field use | **Highest** | Highest after trust | Medium | Low before field base |
| Professional value | High | **Highest information about real value** | Potentially high | Potentially high for portfolio users |
| Trust/safety | **Critical** | High | High | Medium |
| Differentiation | Enabler | Evidence of actual differentiation | **Direct potential** | Direct potential |
| Uncertainty retired | Known defect, decisive proof | **Broadest product uncertainty** | Narrow semantic uncertainty | Narrow portfolio uncertainty |
| Dependency leverage | **Unlocks all real cases** | Selects subsequent work | Depends on field target | Depends on safe small portfolio |
| Cost | Medium | Low-to-medium | Medium-to-high | Low as test |
| Premature-architecture risk | Medium if over-broadened | Low | High | High if productized |
| Speed to real evidence | Fast prerequisite | **Fast after prerequisite** | Slower | Later |

Qualitative order: **1 → 2 → evidence-selected work (possibly 3) → 4 when a real portfolio exists.**

## 9. Recommended next mission

### FIRST REAL-CLIENT LLM EGRESS TRUST GATE

**Mission outcome:** prove that every LLM payload used by the first-client upload/analysis/chat workflow is either protected according to an explicit, tested policy or refused before egress; no silent fallback when protection authority is missing; public wording must not overclaim the proved boundary.

**Strict proposed envelope:**

1. re-verify the active egress call graph on canonical main (the 2026-08-06 inventory is evidence, not assumed current truth);
2. capture deterministic test payloads for primary analysis, verification and Conversation Engine V2;
3. define exact sensitive-data and correspondence-authority invariants, including restart/missing-table behavior;
4. approve the smallest architecture that enforces them without altering financial semantics;
5. implement only the active first-client path after the contract gate;
6. add negative, bypass and static-dependency tests plus independent adversarial review;
7. produce an explicit PASS/FAIL entry criterion for Candidate 2.

It wins because no amount of UX polish, cognition or portfolio prioritisation justifies sending a real client's re-identified financial dossier through a path that contradicts the product's stated protection. The defect is proven, the dependency is independent, the correction unlocks real evidence, and delaying it continues an existing trust exposure.

## 10. Explicitly deferred work

- Portfolio Attention implementation and canonical precedence.
- Materiality ownership policy beyond retaining the unresolved Founder hypothesis.
- Broad onboarding redesign, Living Client Cards and white label.
- General recommendation/attention engines and full internal multi-agent topology.
- Pricing/plan redesign, acquisition optimization and self-hosting.
- Broad integration of every reconstructed cognitive kernel before field evidence selects a need.
- Reconstruction lineage `35444d58d4f8d4576ac7193c66744c561f6cb972` and all recovery archaeology.

## 11. Known uncertainties and Founder arbitration

No Founder arbitration is required to **open the selected mission's read-only contract/current-path phase**.

Before a real-client rehearsal, the Founder will need to approve:

- the case/data package and isolated environment;
- whether the effective super-admin identity should be `fredanciaux@hotmail.com`, `fredanciaux16@gmail.com`, or another explicitly configured identity;
- any residual LLM-provider financial-data exposure that remains by design even after direct identifiers are removed (amounts and patterns themselves can remain commercially sensitive).

These decisions do not justify delaying the selected mission's evidence and contract work.

---

**Control decision:** The canonical product has re-entered normal product development. Recovery remains closed. The next authorized recommendation is the bounded **FIRST REAL-CLIENT LLM EGRESS TRUST GATE**; it must not begin automatically from this artifact.

## 12. Trust Gate execution update — 2026-08-30

- **Current security-lineage HEAD:** `be95c426bcb4f0d5552e36d97a18b0153b7dbb48` on `work/llm-egress-trust-gate-slice-2`.
- **Slice 1:** independently reviewed PASS; implementation `dfdf6b98b3a8572760828a39e682ef7a78b2f624`, review artifact `6aeb01f41924c38c946b2642384047a6c20435dd`.
- **Slice 2:** independently reviewed PASS; final implementation `1b7fe1fa4cfebe60c0d398e4f420704b07d1aa19`, concurrency evidence retained at `be95c426bcb4f0d5552e36d97a18b0153b7dbb48`.
- **OpenAI provider:** conditionally approved candidate only. Real-data admission remains closed pending PG-1–PG-8 evidence/configuration.
- **Milestone A — synthetic Founder rehearsal:** not ready. Ownership isolation is solved, but the actual product path still needs one bounded synthetic-only orchestration/admission implementation and a rehearsal fixture/scorecard.
- **Milestone B — real-client admission:** not ready. Durable correspondence, real provenance, terminal re-identification, task-specific minimization, provider/account policy and explicit real-data admission remain.
- **Exact next mission:** `SYNTHETIC FOUNDER REHEARSAL PATH — BOUNDED EGRESS ORCHESTRATION & RUNBOOK`.
- **Frozen:** Portfolio Attention, broad cognitive/agent/memory redesign, new connectors, plan/UX work and historical recovery.

## 13. Isolated synthetic product sandbox — 2026-08-30

- Milestone A strategy is now source isolation rather than production-security simulation.
- The only admitted source is the byte-exact registered Optilux V3 fixture.
- Production databases, uploads, correspondence, enterprise memory and arbitrary identifiers/content remain outside the sandbox.
- Real-data admission remains closed. RD-1 through RD-5 are preserved in `docs/Security/PEPPERYN_ISOLATED_SYNTHETIC_PRODUCT_SANDBOX_V1.md` as Milestone B blockers.
- Live rehearsal requires only a dedicated development `OPENAI_API_KEY`; absence of that secret does not authorize any alternative transport.

## 14. Sandbox review evidence reconciliation — 2026-08-30

- **Reviewed implementation:** `cefc131481f4beebf6cfd88995d47189db6f894c` on `work/synthetic-founder-rehearsal-v1`; the implementation is unchanged after that commit.
- **Sandbox verdict:** PASS — no S0/S1/S2 or material S3 was reported. The reported review found that neither real nor arbitrary caller data could reach sandbox GPT and that the sandbox did not weaken production security.
- **Evidence classification:** **FOUNDER-SUPPLIED HISTORICAL REVIEW VERDICT**. The previous Work mission reported the verdict, targeted suite (`79 passed`), backend suite (`1466 passed / 12 historical failures / 50 skipped / 3 warnings`), legacy standalone result (`89 OK / 4 historical FAIL`) and `git diff --check` PASS. The standalone original reviewer artifact was not preserved or found in the repository or durable Work outputs. This record preserves the handover without upgrading it to repository-native primary evidence.
- **Re-review decision:** not required solely to recreate paperwork. The exact reviewed commit remains checked out unchanged, and its implementation, tests, sandbox boundary, runbook and scorecard preserve the reported review scope.
- **Live GPT rehearsal:** pending; the scorecard has not been completed.
- **Promotion/deployment:** the sandbox is not merged into `main`, has no upstream branch, and is not evidenced as pushed or deployed.
- **Limitation:** Sandbox V1 begins from the registered byte-exact Optilux V3 `AnalysisResult` fixture after historical ingestion. It does not validate raw-workbook ingestion or a complete `RAW EXCEL → INGESTION → FULL PEPPERYN → GPT` path.
- **Real-data admission:** CLOSED. RD-1 through RD-5 remain preserved and mandatory for Milestone B.
- **Exact next action:** Founder securely configures a dedicated OpenAI development-project credential locally as `OPENAI_API_KEY`, with an appropriate spend limit, and explicitly authorizes the isolated synthetic rehearsal. No credential value may enter Work chat, Git, Dropbox, documentation, tests or logs.

## 15. PC2 local execution environment — 2026-08-31

- **PC2 repository:** `C:\Users\ADMIN-FRED\Documents\Codex\Pepperyn-development` on `work/synthetic-founder-rehearsal-v1` at setup baseline `b5b0e5cd35e8ef10c8df6cda78869a734828cda2`.
- **Repository runtime specification:** deployment evidence uses Python 3.11; runtime dependencies are specified in `backend/requirements.txt`. The repository has no Python-version file, dependency lock, pytest configuration or pinned test dependency.
- **PC2 environment:** external virtual environment `C:\Users\ADMIN-FRED\Documents\Codex\.venvs\Pepperyn-development-backend-py312`, created from the available bundled Python 3.12.13 runtime. It is outside the repository because the security tests intentionally scan every Python file below `backend`.
- **Installed dependencies:** repository runtime requirements plus pytest 9.1.1. This proves PC2 compatibility; it does not change the Python 3.11 deployment baseline.
- **Targeted non-live validation:** `79 passed` across `test_llm_egress_authority.py`, `test_ownership_authority.py` and `test_synthetic_product_sandbox.py`.
- **Backend regression:** `1466 passed / 12 historical failures / 50 skipped / 3 warnings / 6 subtests passed`, matching the preserved sandbox review inventory. The standalone legacy executive-decision script remains `89 OK / 4 historical FAIL`.
- **No-credential rehearsal preflight:** fails closed with `SANDBOX_CONFIGURATION_REQUIRED` and exit code 2; no provider call is made.
- **Current gate:** **FOUNDER EXECUTION GATE**. The only remaining M1A execution prerequisite is a dedicated development-project `OPENAI_API_KEY` configured locally for the single rehearsal process. The credential value must not enter Work chat, Git, Dropbox, documentation, tests or logs.

## 16. M1A OpenAI non-completed response diagnosis — 2026-08-31

- **Observed live result:** the first credentialed synthetic run reached OpenAI and stopped fail-closed as `OPENAI_RESPONSE_NOT_COMPLETED`; no rehearsal output or scorecard was produced. The exposed development credential was revoked.
- **Retrospective root-cause limit:** the original adapter collapsed `incomplete`, `failed`, `in_progress`, `cancelled`, `queued`, unknown statuses and response errors into one code. It did not preserve the safe response status or `incomplete_details.reason`, so the exact original provider condition cannot be recovered without another synthetic run.
- **Strongest evidenced cause:** the sandbox used `gpt-5` at its default medium reasoning effort with `max_output_tokens: 2500`. OpenAI's current Responses API contract counts both reasoning and visible output against that limit and can return `status: incomplete` with reason `max_output_tokens`. The deterministic strict schema permits substantially more visible text than the old budget. This mechanism is demonstrated locally with contract-shaped mock responses, but it is not claimed as retrospectively proven for the original run.
- **Bounded correction:** explicitly set `reasoning.effort: minimal`, raise the sandbox-only output budget to 8,000 tokens, and preserve fail-closed status-specific diagnostics. Only documented incomplete reasons (`max_output_tokens`, `content_filter`) and status classes are surfaced; provider messages and payloads remain suppressed.
- **Contract verification:** `gpt-5` remains compatible with the Responses API, Structured Outputs and `minimal` reasoning under current official OpenAI documentation. The request remains stateless with `store: false` and uses the same strict JSON schema.
- **Validation:** targeted Slice 1 + Slice 2 + sandbox suite `87 passed`; backend regression `1474 passed / 12 historical failures / 50 skipped / 3 warnings / 6 subtests passed`; no-credential preflight remains `SANDBOX_CONFIGURATION_REQUIRED` with exit code 2.
- **Independent adversarial review:** code and security impact PASS; reporting/readiness CONDITIONAL because the original reason is irrecoverable and 8,000 tokens is a bounded operational budget, not a mathematical completion guarantee. Any future incomplete response remains fail-closed with a reason-specific code.
- **Current gate:** a new dedicated development credential and one synthetic Optilux-only rerun are required to confirm whether the correction completes M1A. No real-data admission or production path is changed.

## 17. M1A corrected live synthetic execution — 2026-08-31

- **Evidence classification:** **FOUNDER-SUPPLIED LIVE EXECUTION RESULT**. The Founder executed the reviewed synthetic sandbox locally on PC2 at synchronized branch HEAD `b31825ae40c32963e4c86f700716abe6422ea7c0`; no credential value was supplied to Work.
- **Transport/result:** PASS. The Responses API call completed normally and returned all four strict structured fields: `diagnostic`, `founder_questions`, `recommendations`, and `uncertainties`. The previous generic non-completed failure did not recur.
- **Usage:** `input_tokens: 3877`, `output_tokens: 886`, `total_tokens: 4763`.
- **Credential handling:** the temporary development `OPENAI_API_KEY` was removed after execution and is unavailable. No further live call is authorized or currently required for transport diagnosis.
- **Evidence discipline:** this successful run proves that the corrected sandbox configuration can complete a live synthetic OpenAI rehearsal. It does not retrospectively prove that the former 2,500-token ceiling caused the first failure.
- **Product-evaluation gate:** the repository scorecard remains blank and the non-secret structured field values are not present in repository evidence or the current Work task. API completion is established, but financial usefulness, epistemic discipline, recommendation quality, cognitive relief and differentiation cannot be scored without the returned review text.
- **Current M1A status:** **TRANSPORT PASS / PRODUCT SCORECARD PENDING**. Do not declare full M1A PASS or select M1B versus M1C until the non-secret structured review is evaluated against `PEPPERYN_FOUNDER_SYNTHETIC_REHEARSAL_SCORECARD_V1.md`.
- **Security:** real-data admission remains CLOSED; Slice 1, Slice 2 and RD-1 through RD-5 are unchanged.

## 18. M1A recovered evidence and product-quality evaluation — 2026-08-31

- **Recovered primary evidence:** the Founder-local non-secret capture `founder-synthetic-rehearsal-b31825a-retry2.json` is complete (SHA-256 `F4686ACD2445FC35D2C5D1F5B244F441200CE723D1BA9D3579EA8DEEB7F894A6`, 41,652 bytes). It records `OPTILUX_V3`, the deterministic summary, all four structured review fields and usage of `3877 / 677 / 4554` input/output/total tokens. This is a separate recorded execution from the earlier Founder-reported successful usage of `3877 / 886 / 4763`.
- **Historical evidence preservation:** the earlier empty, configuration-required and unsuccessful capture attempts remain unchanged as execution history. They are not reconstructed, overwritten or treated as product-output evidence.
- **Evaluation verdict:** **M1A TRANSPORT PASS / PRODUCT REVISE**. The Founder questions and uncertainty handling are useful and disciplined. However, the diagnostic is only a meta-description of the review rather than a ranked financial diagnosis, and several financing, employment, factoring and cost-reduction recommendations are framed as directives before their acknowledged evidence gaps are closed.
- **Severity:** no P0 was observed. Two demonstrated P1 defects share one root contract gap: the sandbox did not require a decision-ready, fact-linked diagnosis or require consequential recommendations to name prerequisite validation. Visible provenance/workflow continuity is P2; missing latency/cost evidence is P3.
- **Smallest correction:** strengthen only the sandbox instructions and strict-schema descriptions. Require the main financial tension and exact supplied facts or values; reject a meta-description as diagnosis; keep financing, employment, legal and commercial actions conditional on prerequisite validation; connect Founder questions to diagnosed evidence gaps. Model, token budget, transport, source boundary and production admission are unchanged.
- **Validation:** sandbox-only contract suite `17 passed`; targeted Slice 1 + Slice 2 + sandbox suite `88 passed`; backend regression `1475 passed / 12 historical failures / 50 skipped / 3 warnings / 6 subtests passed`. The 12 failures match the preserved export/rule inventory and are unrelated to this change.
- **Independent adversarial review:** APPROVED with no P0/P1/P2 finding and no security or fail-open regression. P3 observation only: schema descriptions guide semantics but cannot prove model adherence, so the corrected behavior requires the planned single synthetic rerun.
- **Product differentiation:** the observed narrative alone does not materially outperform a capable general-purpose AI given the same dossier. Pepperyn's deterministic financial preparation and source isolation are valuable, but the visible output must demonstrate evidence linkage and governed recommendation discipline before a Fractional CFO can rely on it without substantial rework.
- **Current milestone:** **M1B — observed P0/P1 correction loop**. M1A is not yet a full product PASS. After local regression and independent review, one repeat of the same synthetic Optilux rehearsal is required to validate the corrected product behavior; it is not needed to diagnose or implement the correction.
- **Security:** real-data admission remains CLOSED. Slice 1, Slice 2 and RD-1 through RD-5 are unchanged. No production admission rule, provider payload logging or fail-closed behavior is modified.

## 19. M1B bounded transport-blocker remediation — 2026-08-31

- **Direct retry4 evidence:** the authorized product-evidence rerun stopped fail-closed as `OPENAI_RESPONSE_INCOMPLETE_MAX_OUTPUT_TOKENS`; its JSON output is empty and its stderr evidence has SHA-256 `93DAE3F2D4125E6A8F9C49EB149A53F623BEAA111FD214EDE6BDC45E7C9D4BBD`. This is an execution blocker, not a new product P0/P1 finding, and it supplies no basis to judge the two open P1 corrections.
- **Configuration at `093fa26`:** `gpt-5`, `reasoning.effort: minimal`, strict four-field JSON schema, `store: false`, and `max_output_tokens: 8000`. The schema permits 4,000 diagnostic characters plus three arrays of five 1,000-character items: 19,000 business-content characters before JSON framing.
- **Contract and correction:** OpenAI Responses counts visible output and reasoning tokens together against `max_output_tokens`; the current GPT-5 model limit is 128,000 output tokens. The sandbox-only budget is raised to `32768`, reserving approximately 20k tokens conservatively for the maximum legitimate JSON response and 12k+ for variable reasoning. The model, reasoning effort, schema, required fields, field limits and fail-closed handling are unchanged.
- **Bounded cost:** at the current documented GPT-5 output rate of $10 per million tokens, the maximum generated-token charge permitted by this sandbox ceiling is approximately $0.32768 per run, plus input usage (about $0.00485 for the observed 3,877-token input). Billing remains actual-usage based; the ceiling is not a fixed charge.
- **Validation:** targeted Slice 1 + Slice 2 + sandbox suite `89 passed`; backend regression `1476 passed / 12 historical failures / 50 skipped / 3 warnings / 6 subtests passed`. The 12 failures match the preserved unrelated inventory.
- **Independent adversarial review:** APPROVED with no P0/P1/P2 finding. The correction remains bounded, does not silently truncate, preserves semantic field constraints, security isolation and fail-closed behavior. P3 observation only: the headroom invariant is a defensible policy for French professional output, not a mathematical tokenization proof for every theoretical Unicode string.
- **Single-run rule:** exactly one new Founder synthetic execution is authorized to obtain the missing M1B product evidence. If it again returns `OPENAI_RESPONSE_INCOMPLETE_MAX_OUTPUT_TOKENS`, stop without further budget escalation and escalate the response-design question.
- **Security:** real-data admission remains CLOSED. Slice 1, Slice 2 and RD-1 through RD-5 are unchanged.

## 20. M1B product-evidence closure and M1A final verdict — 2026-08-31

- **Validated execution:** the single authorized rerun at synchronized commit `5de940d081a8ef9a04c6c527246296323896103d` completed with empty stderr and returned all four strict fields. The non-secret evidence `founder-synthetic-rehearsal-5de940d.json` is 49,626 bytes with SHA-256 `9999E6969D7D1EC0390004CAF8D02D42AE8E1006E67C9F7301F068F9414EAF74`; usage is `4063 / 1669 / 5732` input/output/total tokens.
- **P1-1 — resolved:** the diagnostic now explicitly ranks liquidity first, structural unprofitability second and balance-sheet risk third. It grounds those tensions in supplied values and facts including the estimated EUR 45,000 60-day cash shortfall, liquidity score 3/10, fixed costs at 82% of revenue and debt at 78% of equity, while separating observed facts, deterministic calculations and material unknowns.
- **P1-2 — resolved:** consequential actions now identify their prerequisite validations: eight-week forecast and balances for debt negotiation, receivable eligibility for factoring, fixed-cost mapping for the 15% target, aged receivables for collections, schedules/covenants for debt restructuring and validated item margins for product-mix action. Unconditional items are limited to cash-monitoring and governance controls.
- **Scope discipline:** no new P0/P1 is directly demonstrated by this run. No additional product correction or OpenAI execution is justified inside M1B.
- **M1B verdict:** **PASS / CORRECTION LOOP CLOSED**.
- **M1A verdict:** **FULL PASS**. The transport, strict structured response, synthetic boundary and the two demonstrated product-quality corrections have now been validated live on the registered Optilux fixture.
- **Next milestone:** **M1C — synthetic raw-workbook ingestion validation** may begin. Its scope is validation of the synthetic raw-workbook ingestion path; it does not authorize real-client data, deployment, production admission or reopening broader architecture work.
- **Security:** real-data admission remains CLOSED. Slice 1, Slice 2 and RD-1 through RD-5 remain mandatory and unchanged.

## 21. M1C synthetic raw-workbook ingestion validation — 2026-08-31

- **Registered source:** `OPTILUX_M1C_RAW_V1`, a four-sheet professional synthetic workbook containing literal P&L, balance-sheet, cash/BFR and synthetic metadata values. The committed XLSX is 6,577 bytes with SHA-256 `D534C91B9B2AD260526FE029C5DC56F022205EE0BD6C8F9EA0648A5BA1EF8E8D`; it contains no formulas, macros or external links. The sandbox loader accepts no caller path, bytes, entity or identifier and rejects byte mutation before ingestion.
- **Exact path demonstrated without component mocks:** registered XLSX bytes → `validate_excel_before_analysis` → `pepperyn_data_robustness.import_finance_excel` → `FileConnector.fetch` → `file_parser` openpyxl path → `wrap_file_parser_output` → temporal/BFR/balance normalization → `anonymize_parsed_data` → actual `classify_document` call → `dispatch_legacy_synthetic` refusal `REAL_DATA_ADMISSION_CLOSED` before transport.
- **Financial correctness:** current actual period 2025; revenue 2,400,000; EBITDA -145,000; net result -171,000; cash 336,000; BFR 579,000; equity 791,000; total assets and liabilities 2,206,000. Prompt-prioritized summaries carry explicit period `2025`. The synthetic entity is replaced with `ENTREPRISE_001`, while figures and periods are preserved and the correspondence table remains outside the anonymized representation.
- **Falsification:** a test mutates the actual workbook EBITDA cell from -145,000 to -146,000 and observes the real `FileConnector` output change. A byte mutation of the registered workbook is refused before Quality Gate or parser. The parser, payload and anonymizer are not mocked.
- **P1 corrected — current-period grounding:** BFR and balance summaries previously selected the first historical numeric value. They now require the sole `CURRENT_ACTUAL` value, attach period provenance and omit unresolved/ambiguous current periods rather than mislabeling history.
- **P1 corrected — Quality Gate routing:** a partial cash-table ERP match previously overrode explicit P&L/balance statements and blocked this valid structural workbook. Core statements now take structural precedence only when no complete ERP mapping exists; a complete synthetic ERP workbook with a Budget sheet remains `erp_transactional`.
- **Evidence artifact:** `docs/Product/PEPPERYN_M1C_SYNTHETIC_RAW_INGESTION_EVIDENCE.md`; deterministic local runner `python -m sandbox.run_raw_ingestion_evidence`.
- **Validation:** targeted M1C + temporal + Slice 1 + Slice 2 suite `126 passed`; backend regression `1486 passed / 12 historical failures / 50 skipped / 3 warnings / 6 subtests passed`. The 12 failures match the preserved unrelated export/rule inventory.
- **Independent adversarial review:** Architecture, QA and Security APPROVED after two review/correction loops; no P0/P1 remains. Review independently reproduced the registered hash, financial values, mutation falsifier, ERP/structural routing and actual `REAL_DATA_ADMISSION_CLOSED` refusal before transport.
- **P2/P3 backlog:** broaden OOXML package-safety assertions beyond the directly inspected fixture; keep the private dual parsed/anonymized evidence object synthetic-only; optionally check in a regeneration utility in addition to the byte-pinned provenance. None blocks the demonstrated segment or opens real-data admission.
- **Components not demonstrated:** HTTP auth/quota/stream and persistence effects are deliberately bypassed; provider-dependent completion of `run_full_pipeline`, provider-produced `AnalysisResult`, EDM, DecisionKernel and the governed GPT review are not exercised from the raw workbook.
- **M1C verdict:** **LOCAL INGESTION SEGMENT PASS / FULL M1C BLOCKED AT FOUNDER ARCHITECTURE DECISION GATE**. Claiming `RAW WORKBOOK → FULL PEPPERYN → GPT` would be false. The next safe design requires an authorized, receipted transformation contract for provider output between the classification/evidence/analysis/verification stages. A static or manually reconstructed `AnalysisResult` is prohibited as a disguised fixture.
- **Founder decision required:** authorize or decline a bounded synthetic-only architecture mission to design the receipted provider-output transformation needed between the existing multi-stage cognitive calls. This would be a new security/authority contract, not an ordinary test adaptation; it must not imply real-data admission.
- **Security:** the actual first provider stage proves `REAL_DATA_ADMISSION_CLOSED` before transport. Slice 1, Slice 2, LLM Egress Authority, Provider Gate and RD-1 through RD-5 remain unchanged; real-data admission remains CLOSED.

## 22. Autonomous V1 delivery — governed single-call foundation — 2026-09-05

- **Founder decisions recorded:** Excel and PDF remain mandatory for V1; PowerPoint is SHOULD HAVE. Generalized FRU remains deferred, but heterogeneous input must either be understood correctly or stop as explicit `AMBIGUOUS`/`INSUFFICIENT`. A single structured frontier-model call followed by deterministic validation is the preferred V1 orchestration when it preserves Pepperyn's epistemic and isolation invariants.
- **V1 contract:** deterministic source facts are restricted to a small recognized financial metric vocabulary, the sole governed current period, bounded native numeric values, hashed sheet references and stable source-derived fact IDs. Unrecognized labels are not promoted. Locale-ambiguous numeric strings stop before provider dispatch.
- **Provider result contract:** strict structured output keeps observations, dimension assessments, inferences, unknowns, contradictions and recommendations separate. Exact source-representation hash and a per-execution nonce bind every accepted result to its input and invocation. Observations must exactly reproduce their cited metric/value; every reference must resolve; consequential recommendations require evidence or an explicit prerequisite. Responses remain stateless (`store: false`) and all non-completed/refusal/unsupported states fail closed.
- **Durable projection:** `GovernedAnalysisEnvelope` persists the original source facts and governed model output beside the backward-compatible `AnalysisResult`. Narrative diagnosis and score interpretations are visibly marked `INFERENCE`. No confidence is fabricated when none exists. Only high/critical observations enter critical problems.
- **Golden Case extension:** the hash-pinned M1C workbook now deterministically exercises ingestion, normalization, anonymization, governed financial understanding, strict response parsing and a backward-compatible `AnalysisResult` projection; it invokes the existing EDM and DecisionKernel builders to expose compatibility gaps. A serialization round-trip preserves lineage and epistemic categories, but this is not yet a production persistence/restart proof. The provider response is explicitly a deterministic no-network test double; this evidence does not claim live provider execution or product-interface coverage.
- **Falsification result:** the existing EDM can safely display only literal EBITDA/cash/revenue values deterministically projected from governed source facts while the authoritative envelope remains adjacent. It has no safe representation for evidence-linked, prerequisite-bearing recommendations. DecisionKernel correctly returns `None` because its available decisions require canonical-looking scores and its recommendation model loses the V1 prerequisite/evidence contract. Independent review demonstrated that copying provider severity, scores or priority into those fields silently promoted inference. That promotion was removed. Source-referenced dimension assessments remain explicit governed inferences only; a governed downstream consumer is still required before decision/action completion can be claimed.
- **P1 corrections:** provider input is built only from the minimized/anonymized representation; current-period and source-field references use bounded grammars/opaque hashes; conflicting same-period metrics fail `AMBIGUOUS`; unsupported numeric rows create an explicit coverage limitation; the durable envelope stores only governed authority and produces a fresh non-authoritative legacy projection; provider severity, score and priority never populate legacy canonical fields.
- **Current validation:** V1 contract, Golden Case, M1C, Slice 1 and DecisionKernel targeted suite: `147 passed`. Backend regression: `1517 passed / 10 historical failures / 50 skipped / 5 warnings / 6 subtests passed`; the failures match the pre-existing PPTX count, PDF-tooling and EDM-sign inventory. Independent Security and QA confirmation reports no remaining P0/P1 in this bounded scope. Real-data admission remains CLOSED; Provider Gate and RD-1 through RD-5 are unchanged.
- **Git operation:** no active Git process or stale `index.lock` exists; repository ownership and Windows ACLs are correct. The earlier failure was the task sandbox's explicit read-only mount of `.git`, not a repository defect. A turn-scoped, repository-specific metadata permission restores canonical checkpoints without ACL changes.
- **Remaining Golden Case gates:** production persistence/restart, explicit cross-client test at the authenticated product boundary, Excel/PDF output validation, and a fresh-user/founder run entirely through the product interface. A live provider call is not yet required for the deterministic foundation.

## 23. Governed persistence and synthetic application entry — 2026-09-05

- **Persistence contract:** migration v27 adds an immutable governed-envelope record bound by composite database constraints across analysis, authenticated company, entity and engagement. Analysis projection plus authoritative envelope are inserted atomically by a service-role-only transaction; updates, upserts, client table access and legacy `analyse_json` fallback are prohibited.
- **Reload integrity:** application reload filters all four scope identifiers, verifies schema version, envelope hash, contextual binding hash and source-representation hash, then revalidates the frozen governed envelope. Cross-company, cross-entity, cross-engagement and cross-analysis swaps fail closed.
- **Application entry:** an authenticated no-body endpoint runs only the hash-pinned, no-network Golden Case. Its router lives under `sandbox` and is mounted only at non-production startup when explicitly enabled, so production modules retain the sandbox-import prohibition and production OpenAPI has no demo route. The authenticated company must additionally equal the dedicated `PEPPERYN_SYNTHETIC_V1_COMPANY_ID`; synthetic output cannot be written into an arbitrary non-production tenant. A scoped detail endpoint reloads the governed result for product history.
- **Interface entry:** `/app/chat` exposes the synthetic-demo button only when `NEXT_PUBLIC_ENABLE_SYNTHETIC_V1_DEMO=1`. Creating and reopening a governed analysis use authenticated backend endpoints; legacy session loading remains a fallback for non-governed historical analyses.
- **Evidence boundary:** repository tests use HTTP ASGI execution and a reconstructed stateful database double. This proves route/auth/body/serialization and cache-independent application orchestration, not deployment of migration v27 to real Postgres or a real process/database restart. Those remain Internal Pilot acceptance gates.
- **Validation:** focused persistence, migration, entity isolation, HTTP route, sandbox isolation, V1 contract and Golden Case suite: `69 passed`. Backend regression: `1541 passed / 10 historical failures / 50 skipped / 5 warnings / 6 subtests passed`; no new failure remains. Frontend dependencies could not be reconstructed in this Work environment because npm terminated internally before producing a complete `node_modules`; TypeScript validation remains pending in a normal frontend environment.
- **Security:** no provider/egress implementation changed. Real-data admission remains CLOSED. Provider Gate and RD-1 through RD-5 remain unchanged.

## 24. Governed Excel/PDF export after persistence reload — 2026-09-05

- **Export boundary:** the non-production V1 synthetic router now exposes authenticated Excel and PDF downloads for a persisted governed analysis. Both routes re-resolve the designated synthetic company, primary entity and engagement, then reload and cryptographically revalidate the immutable `GovernedAnalysisEnvelope`. Neither route uses the legacy in-memory export caches, reconstructs from `analyse_json`, calls a provider, or builds an `ExecutiveCaseJSON`.
- **Epistemic completeness:** both deliverables render deterministic source facts with period, unit, opaque sheet/field references and source SHA-256; diagnosis and dimension scores are explicitly labelled as inferences; inference confidence and required validations remain visible; UNKNOWN, contradictions, recommendation evidence and prerequisite validation remain separate. The reports explicitly state that AI recommendations are not confirmed decisions.
- **Interface behavior:** governed results select the governed export endpoints in the existing result interface. Non-governed analyses retain the existing production export endpoints. PowerPoint is unchanged and remains SHOULD HAVE.
- **Adversarial correction:** all text beginning with an Excel formula trigger (`=`, `+`, `-`, `@`) is stored as inert text. A regression proves provider-authored `HYPERLINK` content cannot become an executable workbook formula. PDF text is XML-escaped before ReportLab rendering.
- **Restart/isolation evidence:** an HTTP test creates an analysis, reconstructs the database double without process caches, downloads both files, parses their real bytes and checks their governed sections. A second company receives 404 for both formats. This remains a synthetic application test, not proof of deployed Postgres migration or a live multi-process environment.
- **Artifact verification:** the complete 25-fact hash-pinned Golden Case was exported. Artifact Tool inspection found five expected worksheets and zero formula-error tokens; every worksheet was rendered and visually inspected. The two-page PDF was parsed with `pypdf`, rendered with Poppler and visually inspected. Initial unreadable table headers and an unnecessary forced page break were corrected before final validation.
- **Independent review correction:** QA identified a P1 semantic omission: governed observations and their explicitly inferential severity were absent from both mandatory deliverables. Both formats now render the source-matched metric/value separately from the inferential severity, and renderer plus restart-route tests assert those exact categories. No other P0/P1 was found.
- **Validation:** governed export and HTTP route suite `10 passed`; targeted governed-analysis, persistence, migration, M1C, Slice 1/2 and DecisionKernel regression `189 passed`. Backend regression is `1544 passed / 12 historical failures / 50 skipped / 3 warnings / 6 subtests passed`; failures remain confined to the prior PPTX slide-count/content-slice contradictions, legacy EDM sign, and unavailable `pdftotext` inventory. Frontend TypeScript now reaches project checking; the only remaining errors are the pre-existing missing Jest DOM matcher typings in `PortfolioHome.test.tsx` and `ReviewBriefing.test.tsx`, with no error in the changed interface/API files.
- **Dependency observation:** a clean frontend dependency install succeeds when npm uses an isolated writable cache. npm reports 11 dependency vulnerabilities (1 low, 10 high), including a direct warning that Next 14.2.29 requires a security update. This must be resolved or explicitly risk-accepted before a real-client pilot; it does not change the synthetic export evidence.
- **Security:** the routes remain physically absent from production startup and restricted to the dedicated synthetic company. No provider, production admission, retention, tenant policy or real-data path changed. Real-data admission remains CLOSED; Provider Gate and RD-1 through RD-5 remain unchanged.

## 25. Executive deliverables scope clarification — 2026-09-07

- **Critical-path priority unchanged:** complete the Fresh-Founder Golden Workflow first. PDF and Excel remain sufficient for the V1 Internal Pilot; PowerPoint must not delay Founder usability.
- **Real-Client Pilot requirement:** PowerPoint is now **MUST HAVE** for the V1 Real-Client Pilot unless a later explicit Founder decision changes that requirement. This governed PowerPoint slice is scheduled on the path from **Internal Pilot → Real-Client Pilot**.
- **Product roles:** PDF is the concise Executive Analysis for CFO/CEO consumption; Excel is the structured Financial Working Deliverable for deeper professional inspection; PowerPoint is the CODIR / Executive Decision Deck supporting communication and collective decision-making by a Fractional CFO, CFO or CEO.
- **Implementation direction:** do not mechanically restore the historical PPTX. When this post-Internal-Pilot slice begins, inspect the existing implementation, reuse what remains valuable, and evolve it around the current governed Pepperyn analysis contract.
- **Target content, where governed evidence supports it:** executive situation, material financial findings, changes and temporal comparisons, risks and opportunities, uncertainty and limitations, decisions required, confirmed decisions, actions and follow-up, and outlook or points requiring attention.
- **Epistemic invariant:** presentation and visual hierarchy must preserve the distinction among deterministic facts, inference, hypothesis, UNKNOWN, contradictions, recommendations, required validation and confirmed decisions. Formatting may never promote an inference, hypothesis or UNKNOWN into canonical fact.
- **Security/admission:** this scheduling decision does not open real-data admission, deploy production behavior or alter Provider Gate and RD-1 through RD-5.

## 26. Internal-pilot environment reconciliation — 2026-09-07

- **Integration database:** the historical `Pepperyn Integration Test` Supabase project (`ejixkplrgobgwqnhidwt`, never production) was verified structurally current through v26. Canonical v27 was applied once in a transaction and independently verified: exact columns and constraints, composite scope indexes, RLS, zero client policies, immutable-update trigger, fixed-search-path SECURITY DEFINER RPC and empty governed-envelope table all passed.
- **P1 privilege correction:** Supabase public-schema defaults had granted direct table privileges to `anon` and `authenticated`. The integration table was corrected transactionally: `PUBLIC`, `anon`, `authenticated` and `service_role` lose all inherited/direct table privileges; `service_role` regains SELECT only, while creation remains confined to its RPC EXECUTE grant. Full post-remediation verification passed. Schema-wide default privileges remain unchanged because globally removing them could disrupt ordinary Supabase tables; canonical v27 now performs the local revocation for every future application.
- **Frontend security remediation:** the unattributed Next 16/ESLint 9 delta was preserved outside the repository and removed from the working tree. The owned migration targets Maintenance-LTS Next `15.5.24`, React/React DOM `19.2.8`, matching React 19 types, aligned `eslint-config-next`, and a bounded PostCSS `8.5.28` override. A conditional-Hook violation exposed by the first configured lint run was corrected; no product gate or authentication behavior changed.
- **Validation:** isolated clean install passed; production dependency audit reports zero vulnerabilities; lint passes with warnings only; TypeScript passes; all 33 frontend Jest tests pass; and a synthetic-environment production build generates all 24 routes successfully. The v27 migration assertions pass directly in the bundled Python runtime. Independent adversarial review found no remaining P0/P1 in either remediation.
- **Remaining deployment checks:** record a non-production Node runtime at least `20.9`, bind the integration Supabase project and designated company `0e8f3242-4420-4357-b26b-41764c64a97a` without exposing secrets, then execute the Fresh-Founder interface workflow. Real-data admission remains CLOSED.

## 27. Fresh-Founder governed Golden Workflow — 2026-09-07

- **Fresh identity and scope:** the earlier synthetic placeholder user was confirmed to exist in `auth.users` but had no `auth.identities` row; both Auth Admin get/list operations failed closed with `500 unexpected_failure`. It was not repaired through direct Auth-table mutation. The real registration interface instead created a confirmed email identity and a fresh isolated company, default workspace, primary entity and primary engagement for `Optilux Synthetic Internal Pilot`. The integration-only company was explicitly promoted to `pro`; no historical or production account/data was imported.
- **Observed interface path:** authenticated `/app/chat` → no-body `POST /api/v1/synthetic-demo` → deterministic hash-pinned raw Optilux workbook pipeline → governed analysis persistence → visible executive result. The interface displayed the source type `FINANCIAL_WORKBOOK`, deterministic EBITDA and working-capital observations, separately labelled inferential severities, a material UNKNOWN, a prerequisite-bearing recommendation and an explicit statement that AI recommendations are not confirmed decisions.
- **Persistence/restart evidence:** the created analysis `97e23f06-75ca-4b69-8884-8184ce8cf99f` appeared in authenticated history, reloaded through the governed endpoint, survived a complete backend process stop/start, and reopened after a fresh interface reload with the same facts, inference, UNKNOWN and recommendation precondition. This is real Integration Test Postgres/RPC persistence evidence, not the earlier reconstructed database double.
- **Mandatory exports:** the reopened governed analysis returned `200 OK` through the authenticated governed PDF and XLSX endpoints after real persistence reload. The earlier artifact-level regression remains the content/render validation for their five-sheet workbook and two-page PDF; this live interface run validates authenticated Founder access and download transport.
- **P1 compatibility corrections:** the sandbox initially overloaded the legacy `analyses.mode` column with `V1_SYNTHETIC_GOLDEN`, which the canonical database correctly rejected (`quick | complete`). It now stores `complete`; synthetic provenance remains in the immutable envelope, registered workbook name/hash and verification tag. The governed `FINANCIAL_WORKBOOK` type also exceeded the legacy statement vocabulary, so the combined workbook stores the honest legacy projection `AUTRE` while the authoritative governed result remains `FINANCIAL_WORKBOOK`. The route double now enforces both deployed CHECK vocabularies and asserts the projection/authority distinction.
- **Environment findings:** redirected Windows output requires UTF-8 Python I/O because the existing startup banner contains an emoji. A browser served at `127.0.0.1` additionally requires that exact origin in `CORS_ORIGINS`; otherwise preflight fails closed with `400`. Both were corrected as non-production runtime configuration, without broadening production CORS.
- **Adversarial isolation:** targeted executable scenarios pass for (1) a non-designated company attempting to create synthetic history, (2) a second company attempting to reload the first company's analysis, and (3) a second company attempting both governed exports. All return fail-closed `404`; no cross-client result is disclosed.
- **Independent review:** bounded QA and Security reviews APPROVE with no P0/P1. They confirm that `complete` and `AUTRE` are compatibility projections only, while the immutable envelope retains authoritative type, source and tenant bindings; production mounting, provider behavior, fail-closed persistence and real-data admission are unchanged. P2 evidence-maintenance debt remains because the historical `analyses` CHECK vocabularies are observed in deployed schema and asserted in tests but their original DDL is not versioned locally.
- **Validation:** the corrected HTTP create/reload contract passes through the real route with a stateful database double; the three isolation scenarios pass; `git diff --check` reports no defect. The minimal runtime does not contain pytest or pypdf, so these focused functions were executed directly using only the runtime dependencies they require. The full earlier governed suite remains the latest broad regression until the normal development test environment is restored.
- **Observed non-blocking UX debt:** unhandled backend persistence failures surfaced in the browser as the generic `Failed to fetch`, obscuring the server-side fail-closed reason. This did not recur on the validated path and is classified P2; it must be corrected before a real-client pilot, not by weakening exception handling or returning sensitive database details.
- **Internal-pilot status:** the authenticated synthetic Golden Workflow, real persistence/restart, tenant-bound reload, cross-client adversarial isolation and mandatory PDF/XLSX transports are demonstrated. Real-data admission remains CLOSED; the run is deterministic/no-network and does not satisfy the Provider Gate or authorize a real-client pilot.

## 28. Governed CODIR PowerPoint slice — 2026-09-07

- **Bounded implementation:** the synthetic-only V1 router now exposes a governed PPTX download beside the existing PDF/XLSX routes. It authenticates, requires the designated synthetic company, reloads the tenant/entity/engagement-bound immutable envelope and renders without any provider call, legacy export cache or `ExecutiveCaseJSON` reconstruction. Production startup and real-data admission remain unchanged.
- **Executive semantics:** the deck communicates the executive situation, literal source-matched financial observations, separately labelled inferential severity and dimension scores, hypotheses with confidence and required validation, UNKNOWN/contradictions, and proposed decisions with evidence and prerequisites. It states that no recommendation is a confirmed decision. Unsupported temporal comparison, opportunity, confirmed-decision and outlook content is omitted rather than invented.
- **No silent truncation:** an adversarial review found that the first pagination implementation could retain long content in OOXML while clipping it visually. The renderer now divides each contract-valid entry into bounded, character-preserving segments and packs them against the actual content height, creating explicitly titled continuation slides. A maximum-length diagnosis produces nine slides; package integrity and layout geometry report zero findings or warnings, and all rendered slides were visually inspected.
- **Interface behavior:** a governed analysis now sends all three exports to the governed endpoints. Legacy analyses retain their existing PDF/PPTX/XLSX routes. Restart and cross-company route tests now cover PPTX as well as PDF/XLSX.
- **Validation:** governed renderer and HTTP route suite `12 passed`; targeted V1, M1C, persistence, ownership, Slice 1, migration and DecisionKernel regression `284 passed`; frontend TypeScript passes; frontend Jest `33 passed`; `git diff --check` reports no defect. The ordinary six-slide Optilux deck and a nine-slide maximum-diagnosis deck were rendered with the bundled presentation renderer and visually inspected.
- **Independent review:** QA and Security each identified the initial long-content clipping P1; Security then identified that continuation chunks needed to repeat their epistemic classification. Both defects were corrected and independently re-reviewed. Final verdict: no remaining P0/P1; tenant isolation, immutable-envelope validation, provider absence, sandbox-only mounting and closed real-data admission remain intact.
- **Provider Gate freshness:** current official OpenAI documentation still states that ZDR requires approval, is configured at organization/project level, changes Responses `store` behavior to false, and that European regional processing uses a region-specific project/domain with additional retention-control prerequisites. The existing gate therefore remains correctly closed pending effective Pepperyn account/project, DPA, ZDR, region, endpoint/model and code-enforcement evidence.
- **Security/admission:** no provider transport, production mounting, database admission, secret handling or real-client path changed. Slice 1, Slice 2, LLM Egress Authority, Provider Gate and RD-1 through RD-5 remain mandatory; real-data admission remains CLOSED.

## 29. Provider Gate PG-8 executable enforcement — 2026-09-07

- **Enforcement boundary:** every admitted request reaching `LlmEgressAuthority` must now carry a provider-policy authorization issued from deployment configuration and verified immediately before the sole transport boundary. The capability binds policy version, provider, project, base URL, endpoint, model, region, retention, task, exact canonical request hash, evidence hash and expiry. Invalid retry bounds are rejected before either provider or ownership capability consumption.
- **Evidence contract:** deployment configuration alone cannot mint authorization. A non-secret evidence manifest of at most 64 KiB must exist, its raw SHA-256 must match the configured fingerprint, and its complete policy/project/DPA/ZDR/region/model/date tuple must exactly match deployment configuration. Verification dates must be temporally valid and the evidence lifetime cannot exceed 90 days. The request must use the approved model and `store=false`.
- **Adversarial corrections:** initial review demonstrated that `dataclasses.replace()` could mutate policy dimensions while retaining the seal. The final implementation registry-binds the complete tuple. A second review found that embedding the issuer exposed its mint method; authorization now carries only an opaque issuer discriminator, issuer registries remain module-private, production-source scans forbid issuer access, and expired entries are pruned. Tamper tests cover provider, project, URL, endpoint, model, region, retention, evidence and expiry.
- **Validation:** focused provider-policy, egress and ownership suite `98 passed`; final targeted V1/M1C/persistence/migration/DecisionKernel regression `311 passed`; `git diff --check` reports no defect beyond line-ending notices. Independent QA and Security re-review found no remaining P0/P1 and approved PG-8 code enforcement for the bounded closed-transport architecture.
- **Gate interpretation:** **PG-8 code-enforcement implementation is locally complete, but operational activation remains CLOSED.** The manifest proves that deployed configuration matches an approved evidence artifact; it cannot independently prove the provider-account facts or the truth of that artifact. PG-1 through PG-7, effective account verification, transport activation and explicit real-data admission remain mandatory blockers.
- **Security/admission:** `_dispatch_final_request` remains unconditionally closed; no API key is read, no network transport is added and no production caller can mint synthetic admission. Real-data admission remains CLOSED; Slice 1, Slice 2 and RD-1 through RD-5 remain unchanged.

## 30. Provider-account checkpoint and bounded interface failure UX — 2026-09-07

- **Effective organization evidence:** Founder inspection confirms that all
  three OpenAI API data-sharing categories are disabled. PG-1 is therefore
  PASS against the current organization rather than documentation alone.
- **External account blocker:** only a Global default project exists, with no
  retention profile; the create-project form offers no geography selector.
  ZDR and a region-specific European project cannot be evidenced or configured
  through the current account UI. A supported eligibility request is pending
  with OpenAI. No substitute Global project was created.
- **Contract verification:** current official OpenAI documentation continues
  to support the PG-8 shape: Responses API, Structured Outputs, explicit
  `store=false`, and European processing for supported models, conditional on
  effective account and retention eligibility. PG-2, PG-3, PG-4, PG-6 and PG-7
  remain incomplete; PG-8 operational activation remains closed.
- **Bounded P2 correction:** the Fresh-Founder synthetic action no longer
  exposes the browser-native `Failed to fetch` string when the backend is
  unreachable. It emits a stable French recovery instruction instead, while
  preserving deliberate bounded HTTP refusal details and all fail-closed
  backend behavior.
- **Validation:** dedicated network-refusal and bounded-HTTP-refusal tests pass;
  the complete frontend suite is `35 passed`; TypeScript passes; repository
  lint passes with pre-existing warnings only; and the production dependency
  audit reports zero vulnerabilities. The first dependency reconstruction hit
  npm's known internal exit-handler failure and was replaced by a clean,
  lockfile-exact install; no tracked dependency file changed.
- **Security:** the UI translation logs no payload, response or credential and
  does not convert any failure into success. Real-data admission remains
  CLOSED.

## 31. V1 heterogeneous financial-input falsification — 2026-09-07

- **Scope:** four byte-real, hash-pinned synthetic XLSX fixtures exercise the
  actual Data Quality Gate, `FileConnector`, parser/temporal normalization,
  anonymization and governed financial-understanding builder. They are not
  reconstructed parsed objects and do not reuse the Optilux layout.
- **Positive interpretation:** an English two-sheet management workbook with
  reordered content produces ten exact current-period facts, including revenue,
  EBITDA, cash and working capital, with source references and period 2025.
- **Safe insufficiency:** ambiguous period headings, locale-ambiguous numeric
  strings and conflicting current-period revenue each produce a specific
  `AMBIGUOUS` result, promote zero facts and prevent provider-request
  construction.
- **Security boundary:** no provider call, credential, HTTP route, persistence
  or real datum is used. The understood case produces only a no-network,
  `store=false` request object; the three unsafe cases stop before that point.
- **Remaining compatibility debt:** `FY25 Actual` is conservatively left
  unclassified by raw temporal parsing. Because the behavior is explicit and
  fail-closed, it is P2 rather than a V1 P0/P1; generalized FRU remains deferred.
- **Evidence:** `docs/Product/PEPPERYN_V1_HETEROGENEOUS_INPUT_FALSIFICATION.md`
  and `backend/tests/test_v1_heterogeneous_workbooks.py`.
- **Validation:** heterogeneous fixtures plus adjacent M1C, data-quality,
  parser-period, governed-contract, Slice 1/2 and provider-policy suite
  `148 passed`. Every worksheet was
  rendered and visually inspected; package scans found no formula, macro or
  external link. Repository-wide collection remains unavailable in this local
  environment because the historical standalone EDM script exits during
  collection and the development venv lacks optional `pypdf`; neither affects
  this bounded pipeline evidence.

## 32. Closed interface rehearsal for heterogeneous workbooks — 2026-09-08

- **Interface path:** when the non-production V1 synthetic flag is enabled,
  `/app/chat` exposes a distinct XLSX selector for the four registered
  heterogeneous fixtures. It does not reuse or silently redirect into the
  ordinary `/api/analyze` real-file path.
- **Byte-exact admission:** the backend accepts only an exact registered
  filename and SHA-256 pair, reads at most 1 MB and requires the authenticated
  designated synthetic company. A renamed registered file, altered bytes,
  unknown input or another company is rejected before parsing.
- **Bounded result:** the route executes the real local quality gate, parser,
  connector, anonymizer and governed-understanding builder. It returns only
  UNDERSTOOD/AMBIGUOUS/INSUFFICIENT, current-period facts or explicit UNKNOWNs,
  and the literal marker `provider_dispatch: CLOSED`. It neither builds a
  provider request nor persists an analysis.
- **Interface semantics:** the positive workbook reports period 2025 and ten
  governed facts. Each unsafe workbook reports AMBIGUOUS with its explicit
  reason and zero promoted facts. The interface states that no provider call
  occurred; ordinary real-file upload remains separate and unchanged.
- **Validation:** dedicated HTTP scenarios cover the registered positive case,
  all three ambiguous cases, rename/unknown rejection and company isolation;
  together with the fixture pipeline they pass `15 passed`. Frontend Jest
  passes, TypeScript passes and lint reports only pre-existing warnings.
- **Security/admission:** the route remains absent from production and from any
  environment without the explicit synthetic flag. Provider transport and
  real-data admission remain CLOSED.

### Founder-observed five-file interface closeout

- **Execution surface:** the Founder used the actual authenticated `/app/chat`
  file selector against the development backend mounted from checkpoint
  `20aba47fc68c4c64724e3e0a0ea4fb9982782126`. Health, route discovery and the
  multipart CORS preflight were independently confirmed `200` before the final
  run.
- **Registered English workbook — PASS:**
  `pepperyn_v1_heterogeneous_english.xlsx` displayed UNDERSTOOD for period 2025
  with ten governed facts and the explicit statement that no provider call was
  performed.
- **Ambiguous period workbook — PASS:**
  `pepperyn_v1_heterogeneous_ambiguous_period.xlsx` displayed AMBIGUOUS with
  `Exactly one CURRENT_ACTUAL column could not be established.` No fact was
  promoted and the interface stated that no provider call was performed.
- **Ambiguous number workbook — PASS:**
  `pepperyn_v1_heterogeneous_ambiguous_number.xlsx` displayed AMBIGUOUS for the
  registered locale-ambiguous numeric representation, promoted no fact and
  performed no provider call.
- **Conflicting metric workbook — PASS:**
  `pepperyn_v1_heterogeneous_conflict.xlsx` displayed AMBIGUOUS with
  `Conflicting values exist for a governed metric in the current period.` No
  fact was promoted and no provider call was performed.
- **Unregistered renamed copy — PASS:**
  `pepperyn_v1_heterogeneous_english_UNREGISTERED.xlsx` displayed the bounded
  refusal `Fichier refusé : sélectionnez uniquement un classeur synthétique V1
  enregistré.` This exact branch returns HTTP 400 before the quality gate,
  parser, anonymizer or understanding builder. The endpoint has no persistence
  or provider-request operation, so the rejected file produced neither an
  analysis record nor provider egress.
- **Operational diagnostics:** the rehearsal first exposed three local startup
  configuration failures rather than product-contract failures: the public
  Supabase key was absent from the compiled frontend, `JWT_GUEST_SECRET` was
  absent from the backend, and the designated synthetic company identifier did
  not match the authenticated company. Each condition failed closed. After
  correcting only local environment variables and restarting, frontend health,
  backend health, authenticated adjacent endpoints and CORS were healthy; no
  code or admission rule was weakened.
- **Closeout verdict:** all five real interface selections PASS. The positive
  case promotes only deterministic source-referenced facts; all unsafe inputs
  fail closed with zero promoted facts; the unregistered copy is rejected
  before parsing. Provider transport, production mounting and real-data
  admission remain CLOSED.

## 33. Uploaded-workbook governed analysis with local mock provider — 2026-09-08

- **Critical-path purpose:** close the gap between the proven uploaded-file
  understanding path and the already proven governed persistence/render path
  without waiting for OpenAI account eligibility. The non-production interface
  now exposes a separate action for the registered English workbook.
- **End-to-end boundary:** exact filename plus SHA-256 admission → real quality
  gate/parser/anonymizer → ten governed 2025 facts → bounded `store=false`
  request construction → deterministic local mock response → invocation/source
  lineage validation → governed envelope → tenant/entity/engagement-bound
  persistence → ordinary governed result rendering and reload.
- **Mock semantics:** the English mock is distinct from Optilux and does not
  reuse its negative-EBITDA narrative. It cites the positive English-workbook
  facts, separates literal observations from inference, carries confidence and
  validation requirements, preserves a material UNKNOWN, and proposes no
  confirmed decision.
- **Fail-closed admission:** only
  `pepperyn_v1_heterogeneous_english.xlsx` may enter this path. Renamed,
  altered, unknown and all three AMBIGUOUS registered workbooks are refused
  before mock analysis. The route remains designated-company-only, size
  bounded, development-only and absent unless the synthetic flag is enabled.
- **No external provider:** the mock is an in-process mapping with no transport
  dependency. The built request uses model marker `mock-v1-local` and
  `store=false`; it is validated but never dispatched. Persisted token and cost
  fields are zero.
- **Validation:** the focused upload/inspection/mock route and heterogeneous
  workbook suite passes `20 passed`; the adjacent governed-contract selection
  passes `45 passed`; frontend Jest passes `39 passed`; TypeScript passes.
  Tests cover exact-file admission, all three ambiguity refusals, tenant
  isolation, request invariants, governed lineage and persistence arguments.
- **Admission status:** this closes an internal V1 convergence dependency only.
  It does not satisfy or bypass the Provider Gate. External Provider remains
  CLOSED and Real-data admission remains CLOSED.

## 34. Persisted mock analysis and governed export closeout — 2026-09-08

- **Founder persistence rehearsal — PASS:** the Founder ran the registered
  `pepperyn_v1_heterogeneous_english.xlsx` once through the actual interface,
  then restarted the backend and reloaded the same analysis from history
  without creating another analysis. The governed facts, material `UNKNOWN`,
  conditional action plan, validation requirements and distinction between an
  AI recommendation and a professional decision all survived the restart.
- **Durable analysis identity:** the persisted analysis and every final export
  carry the complete UUID
  `75132a71-c80c-4469-aba8-5171d947a9d0`. The source representation SHA-256
  remains `9E23C4CE2B3C8F3F9C67E5DDAF609EF2EC44EB56492563BDADF7E296906785B7`
  in all three formats.
- **Initial export rehearsal and remediation:** the first three downloads were
  structurally valid and mutually consistent but lacked the durable analysis
  identifier and explicit mock/no-network provenance inside the artifacts.
  The PowerPoint also used the unsafe heading `Decisions requises`, and the PDF
  began its second page with an orphaned validation line. The three bounded
  renderers now require the analysis identifier, disclose `Fournisseur simulé
  local` and `Aucun réseau externe n'a été utilisé`, preserve the synthetic-data
  label, use `Recommandations proposées`, and keep each PDF inference with its
  validation block.
- **Founder-observed final export files — PASS:** without creating a new
  analysis, the Founder downloaded and the final QA inspected exactly:
  `pepperyn_analyse_75132a71(1).xlsx`,
  `pepperyn_analyse_75132a71-1.pdf`, and
  `pepperyn_analyse_75132a71(1).pptx`. The older unsuffixed files were excluded
  from the final verification.
- **Artifact validity and content:** the XLSX and PPTX OpenXML packages pass
  archive integrity checks and contain no external links or embedded external
  content. The workbook contains the five expected sheets and no formulas. The
  PDF is a valid, unencrypted two-page A4 document with no JavaScript. Across
  all formats, the same ten source facts, fact references, inferences,
  confidence levels, validation requirements, recommendation and material
  `UNKNOWN` are present; no recommendation is represented as a confirmed
  decision.
- **Visual verification:** all five workbook sheets, both PDF pages and all six
  PowerPoint slides were rendered and inspected. Accented French text is
  correct, the PDF page break no longer separates a validation from its
  inference, and the deck has no detected overflow or truncated content.
- **Automated validation:** focused governed export and persisted-route tests
  pass `12 passed`. The final persistence, export, Provider Gate, migration,
  governed-contract, Golden Case, heterogeneous-workbook and synthetic-route
  selection passes `135 passed`. The first broader attempt encountered only a
  local pytest temporary-directory permission error; rerunning the identical
  selection with an explicit isolated base directory passed completely.
- **Closeout verdict:** governed XLSX/PDF/PPTX export from the existing
  persisted V1 mock analysis is closed with final PASS. No provider request was
  dispatched and no real datum was admitted. External Provider remains CLOSED
  and Real-data admission remains CLOSED.

## 35. Governed Founder-intention capture — 2026-09-08

- **Critical-path purpose:** extend the persisted synthetic Founder rehearsal
  from recommendation delivery to one explicit human response, without
  manufacturing a DecisionKernel, a confirmed professional decision or a
  provider-dependent result.
- **Governed boundary:** recommendations are projected from the integrity-
  checked `GovernedAnalysisEnvelope` with deterministic identifiers. The V1
  endpoint reloads the tenant/entity/engagement-bound envelope and resolves the
  submitted identifier against the server-side recommendation snapshot; the
  browser cannot supply or replace the authoritative recommendation text.
- **Semantic boundary:** the interface asks `Quelle est votre intention ?` and
  states that the response is an intention, never a confirmed decision. The V1
  write path records feedback only and returns `decision_confirmed=false` and
  `arc_created=false`; it does not invoke the legacy DecisionKernel/DecisionArc
  promotion path.
- **Registry repair:** the first reload correctly failed closed at the new
  secondary feedback lookup with PostgREST `PGRST205`, proving that
  `public.decision_feedback` was absent from the deployed schema cache. V28
  restores only that idempotent registry, its exact status constraint including
  `unsure`, unique recommendation/report key, indexes, update trigger and RLS
  policy. It inserts or backfills no status and changes neither analyses nor
  governed envelopes. The migration was applied before the write rehearsal.
- **Availability correction:** a secondary feedback-registry read failure no
  longer hides an otherwise valid governed analysis. The verified analysis and
  recommendations remain readable with feedback state represented as unknown;
  writes continue to fail closed independently. This correction was exercised
  with an explicit registry-outage test.
- **Founder-observed rehearsal — PASS:** on persisted analysis
  `75132a71-c80c-4469-aba8-5171d947a9d0`, the Founder selected exactly
  `Je ne sais pas encore`. The dedicated intention endpoint returned HTTP 200.
  Reopening the same analysis without a new analysis or write displayed
  `Intention enregistrée — aucune décision confirmée.` No further
  `[V1 INTENTION]` warning appeared after V28.
- **Automated validation:** the final targeted backend selection passes
  `32 passed`; frontend Jest passes `41 passed`; TypeScript passes. Tests cover
  deterministic recommendation projection, server-side snapshot enforcement,
  forged recommendation refusal, no-arc semantics, persistence/reload,
  registry-outage isolation and the narrow/idempotent V28 contract.
- **Closeout verdict:** the governed recommendation-to-Founder-intention link is
  closed with PASS for the synthetic V1 rehearsal. Decision/action confirmation
  and later consequence learning remain separate future links. No provider
  request was dispatched and no real datum was admitted. External Provider
  remains CLOSED and Real-data admission remains CLOSED.

## 36. Governed recommendation context before decision — 2026-09-08

- **Corrective purpose:** the intention card initially projected only the
  recommendation action. Although it never claimed a confirmed decision,
  proceeding toward decision capture without the governed rationale,
  prerequisite validations and evidence references would have discarded a
  material part of the V1 recommendation contract.
- **Bounded correction:** the existing deterministic recommendation projection
  now carries the verbatim governed rationale, prerequisite-validation list and
  fact identifiers alongside the action. The interface renders all three before
  any response controls under the explicit labels `Pourquoi cette
  recommandation`, `Validations requises avant toute décision` and `Références
  factuelles`.
- **Semantic invariants:** the correction does not alter the persisted
  intention, create a decision or DecisionArc, change the governed envelope, or
  invoke a provider. A recommendation remains a proposal; prerequisites remain
  required; fact identifiers remain references rather than new assertions.
- **Founder-observed visual rehearsal — PASS:** after backend restart, hard
  refresh and reopening persisted analysis
  `75132a71-c80c-4469-aba8-5171d947a9d0`, the existing `unsure` intention
  displayed the rationale, required validations, factual references and
  `Intention enregistrée — aucune décision confirmée.` No new intention or
  analysis was created.
- **Automated validation:** the targeted backend selection passes `39 passed`;
  frontend Jest passes `41 passed`; TypeScript passes. Tests bind the projected
  fields to the authoritative envelope and verify their governed rendering.
- **Closeout verdict:** the full governed recommendation context is now visible
  before any future professional-decision interaction. External Provider
  remains CLOSED and Real-data admission remains CLOSED.
