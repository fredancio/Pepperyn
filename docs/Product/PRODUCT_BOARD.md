# PRODUCT_BOARD.md

**Nature :** Product Board canonique unique de Pepperyn. Reconstruit selon la stratégie C recommandée par `Audit/PRODUCT_BOARD_CANONICAL_ARBITRATION.md` (2026-08-06), promu ici comme référence unique lors de la Consolidation du dépôt canonique (2026-08-07), après vérification que `main` n'avait pas changé sur ce périmètre depuis l'arbitrage. L'ancienne version (`docs/Architecture/PRODUCT_BOARD.md`) est archivée sous `Archive/PRODUCT_BOARD_PRE_CONSOLIDATION.md` et n'a plus d'autorité.

---

## 1. DELIVERED AND MERGED

*(Ce qui existe réellement dans `main`, vérifié par code + tests + tags — jamais par déclaration.)*

- **Portfolio Intelligence** — Incréments 1 et 2 livrés et fusionnés (`ae79a9e`, `771e7ae`), Closed-Only fix (`bc98187`). `frontend/components/chat/PortfolioHome.tsx`, `ReviewBriefing.tsx`, routes `GET /api/portfolio`, `GET /api/review-briefing`. Tags : `portfolio-home-mvp-validation-complete`, `portfolio-intelligence-mvp-inc-1-2-complete`.
- **Portfolio Home Product Validation Report** — PASSED WITH MINOR RESERVATIONS. 48/48 tests ciblés, 943 passed / 8 échecs préexistants / 1 skip en suite complète, 30/30 frontend.
- **Anonymisation Layer 1** — `anonymization_service.py`, mécanisme correct pour son périmètre déclaré (voir `ANONYMIZATION_CAPABILITY_REVIEW.md` — couverture incomplète, traité en réserve, pas en negation de ce qui est livré).
- **Modèle Company/Entity/Workspace, PIN login, Billing/Stripe, exports PDF/PPTX/XLSX** — confirmés réels et câblés par `LEGACY_CAPABILITY_INVENTORY.md`.
- **T1C-A (Evidence Ledger — capture)** — ADR-001/001A ACCEPTED, fusionné dans `main` le 2026-08-07 (commit `3b1b21a`, revue adversariale passée, verdict A — MERGE AS-IS). **COMPLETE.**
- **T1C-B (Evidence Ledger — faits atomiques)** — ADR-001 ACCEPTED, fusionné dans `main` le 2026-08-07 (commit `0741a03`, revue adversariale passée, verdict A — MERGE AS-IS). `amount`/`currency`/`fact_id` atomiques lus directement depuis la sortie structurée du LLM, `fact_id` = empreinte de contenu déterministe (voir réserve explicite : ce n'est PAS une identité métier, `Audit/STRATEGIC_DEFERRED_WORK_REGISTER.md` §1.3). Table `evidence_ledger_entries` toujours strictement additive, non lue par aucun chemin de production (ADR-001 §8). Baseline post-merge : 988 passed / 8 échecs préexistants / 1 skip. **COMPLETE.**
- **T2A (Engagement Foundation)** — ADR-002 ACCEPTED, fusionné dans `main` le 2026-08-07 (commit `f5f00bf`, revue adversariale passée, verdict A — MERGE AS-IS). Table `engagements`, création atomique Entity+Engagement (chemin applicatif + inscription), backfill idempotent, 40 tests. Baseline post-merge : 1028 passed / 8 échecs préexistants / 1 skip. **COMPLETE.**

  **Décision de cardinalité Entity:Engagement (arbitrage du 2026-08-07, ADR-002 §0) :**
  - **DOMAINE :** une Organisation peut avoir plusieurs Engagements au cours de sa vie (`1:N` permis) — l'identité d'un Engagement suit la continuité du mandat professionnel, pas la durée de vie de l'Organisation.
  - **IMPLÉMENTATION AUJOURD'HUI :** la contrainte `1:1` (`UNIQUE(entity_id)`) reste en place, temporairement, parce qu'aucun comportement de création d'un second Engagement n'existe encore dans le code. Ce n'est plus un invariant de domaine — c'est une contrainte transitoire, avec son déclencheur de relâchement enregistré dans `Audit/STRATEGIC_DEFERRED_WORK_REGISTER.md` §1.2.a.
  - Ces deux affirmations ne se contredisent pas : la seconde est un état d'implémentation actuel, la première est la vérité de domaine qui le remplacera quand un besoin réel apparaîtra.

- **Evidence Ledger Consumer #1 (Review Briefing)** — premier consommateur de production de l'Evidence Ledger, fusionné dans `main` le 2026-08-07 (`ed5cdd7`, `718de9a`, corrigé par `eac6ba2`, merge `db8892e`). Revue adversariale pré-fusion passée (wording UI corrigé pour ne jamais impliquer une justification au niveau assertion — uniquement un contexte au niveau analyse ; qualificatifs de provenance ne prétendent jamais une vérification indépendante). Aucun fallback vers `analyse_json`, `fact_id` jamais lu ni exposé, zéro nouvel appel LLM. Baseline post-merge : 1061 passed / 8 échecs préexistants / 1 skip. **COMPLETE.**
- **Persistence observability (Evidence Ledger)** — `GET /api/admin/evidence/integrity`, `evidence_integrity_service.py`, fusionné avec Consumer #1. Signal agrégé de comptage, explicitement reframé "observability" (pas "integrity gate") suite à la revue adversariale — ne bloque rien, ne corrige rien, ne classe jamais par ligne. **COMPLETE, TEMPORARY** : la classification par ligne (pré-Ledger / capture vide / échec d'écriture) reste un gap nommé, voir section 4.
- **DecisionArc ↔ Engagement (rattachement relationnel)** — fusionné dans `main` le 2026-08-08 (`b99810a`, corrigé par `f05b0bd`, merge `8db6070`). Revue adversariale passée (verdict B, corrections appliquées). `decision_arcs.engagement_id` (nullable, additif) résolu de façon strictement déterministe (`origin_analysis_id → analyses.entity_id → engagements.entity_id`), jamais une heuristique. DecisionArc reste son propre agrégat racine ; Engagement est une ancre de continuité (`ON DELETE SET NULL`, pas une composition). Backfill historique idempotent disponible. Baseline post-merge : 1076 passed / 8 échecs préexistants / 1 skip. **COMPLETE.**
- **Decision Memory Integrity Repair** — répare les deux défauts d'intégrité préexistants découverts par la revue ci-dessus (non introduits par elle), revue adversariale finale passée (verdict B, corrections documentaires appliquées), branche `implementation/decision-memory-integrity-repair-2026-08-08`, prête à fusionner. `decision_arcs.entity_id` est désormais peuplé à la création (résolu depuis `analyses.entity_id`, jamais fabriqué) — Portfolio Intelligence et le filtre `entity_id` du Briefing de revue sont restaurés. `origin_analysis_id` est nullable (migration `v22`) : provenance historique, pas une identité — un DecisionArc survit à la suppression ordinaire de son Analysis d'origine (`origin_analysis_id → NULL`). Seule l'érasure complète d'une company (RGPD, `DELETE /account`) détruit encore le DecisionArc, à l'échelle de la company. Validé empiriquement sur un vrai moteur Postgres (projet Supabase dédié « Pepperyn Integration Test »). **COMPLETE.**
- **FTE v0 (Financial Time Engine — kernel déterministe minimal)** — `docs/Architecture/FTE_MINIMAL_IMPLEMENTATION_CONTRACT.md` (contrat rétréci deux fois par arbitrage explicite), fusionné dans `main` le 2026-08-08 (`b5a163a` corrections, merge `2e34aef`). Revue adversariale indépendante pré-fusion passée (verdict B — corrections appliquées avant fusion : précédence `UNKNOWN` dans `classify_period_relationship`, tri par temps métier dans `resolve_previous_observed_period_end`). Cinq états de comparaison déterministes (`NEW`/`DUPLICATE`/`GAP`/`OUT_OF_ORDER`/`UNKNOWN`), un seul champ persisté (`evidence_ledger_entries.observed_period_end DATE NULL`, migration `v23`, validée sur Postgres réel), YTD et rolling-12 honnêtes, zéro appel LLM, `QuantifiedImpact.temporal_role` isolé (test AST). Baseline post-merge : 1147 passed / 8 échecs préexistants / 1 skip. **FTE KERNEL COMPLETE.**
- **Knowledge Model v0 (CONFIRMED enterprise knowledge, sujet unique `EXPENSE_SIGN_CONVENTION`)** — `docs/Architecture/Cognitive/KNOWLEDGE_MODEL_V0_IMPLEMENTATION_CONTRACT.md`, fusionné dans `main` le 2026-08-08 (`773cb0b`). Revue adversariale indépendante pré-fusion passée (verdict B → corrections appliquées avant fusion : migration `v25`, `UNIQUE(relates_to_knowledge_id)`, ferme le branchement après une première confirmation). Table `knowledge_model`, vrai propriétaire = Entity (jamais Engagement), CONFIRMED-only (aucune ligne CANDIDATE persistée), insert-only, zéro LLM. Interface exclusive : `confirm()`/`recall()` (`services/knowledge_model_service.py`). **Correction complémentaire — unicité de racine (2026-08-09)** : migration `v26` (`UNIQUE(entity_id, subject) WHERE relates_to_knowledge_id IS NULL`) ferme la course de *première* confirmation concurrente (deux racines contradictoires pour un `(entity_id, subject)` neuf — gap nommé par la revue architecturale Epistemic Dialogue v0, distinct de ce que `v25` couvrait déjà). Défaut prouvé et correction validée sous vraie concurrence (deux threads réels) contre PostgreSQL local et sur Pepperyn Integration Test ; fusionné dans `main` (`a7a1049`, verdict A — STRUCTURALLY CLOSED). 46 tests. **COMPLETE.**

  **Réserve explicite, à ne jamais lire comme résolue par la ligne ci-dessus :** la normalisation réelle des en-têtes de période du cas Phidani **n'est PAS complète**. Le fichier réel `Phidani.xlsx` utilise des en-têtes `YYYY-MM` que `temporal_normalizer.py` ne résout pas encore (gap connu, documenté, testé) — le kernel FTE répond honnêtement `UNKNOWN` sur ce fichier plutôt que de fabriquer une classification, ce qui est le comportement correct, mais ce n'est pas une démonstration positive de bout en bout du cas vertical Phidani. Cette réparation reste un chantier séparé, étroitement scopé — voir `Audit/STRATEGIC_DEFERRED_WORK_REGISTER.md` §1.4.a.

## 2. ACCEPTED BUT NOT YET MERGED

*(Rien en code actuellement — ADR-003 v3 est superseded par `docs/Architecture/FTE_MINIMAL_IMPLEMENTATION_CONTRACT.md`, contrat canonique désormais fusionné dans `main`, dont FTE v0 ci-dessus est la première implémentation.)*

**Architecture canonique** — fusionnés dans `main` le 2026-08-08 (`ea4a137`, `3be19e4`) :
- **FRU FOUNDATION** (`docs/Architecture/Cognitive/FINANCIAL_REPRESENTATION_UNDERSTANDING_FOUNDATION.md`) — CANONICAL, corrections doctrinales chirurgicales le 2026-08-09 (propriété Entity, pas Engagement — voir ligne Knowledge Model v0 ci-dessus). IMPLÉMENTATION NON COMMENCÉE.
- **EPISTEMIC DIALOGUE FOUNDATION** (`docs/Architecture/Cognitive/EPISTEMIC_DIALOGUE_FOUNDATION.md`) — CANONICAL, mêmes corrections doctrinales le 2026-08-09. IMPLÉMENTATION NON COMMENCÉE.

*(KNOWLEDGE MODEL V0 CONTRACT n'apparaît plus ici — implémenté et fusionné, voir section 1.)*

**Écrit, revu, PAS ENCORE FUSIONNÉ** :
- **EPISTEMIC DIALOGUE V0 IMPLEMENTATION CONTRACT** (`docs/Architecture/Cognitive/EPISTEMIC_DIALOGUE_V0_IMPLEMENTATION_CONTRACT.md`) — branche `architecture/epistemic-dialogue-v0-2026-08-09` (`eead286`), verdict B — READY WITH NAMED RESERVATIONS. `ClarificationNeed` (4 champs, non persisté), RECALL-before-ASK structurellement testable, matérialité par sujet (pas de moteur de score), zéro LLM en v0, chat = surface de présentation uniquement. La réserve nommée (course de première confirmation concurrente sur Knowledge Model) est désormais fermée par la correction ci-dessus (section 1) — le contrat lui-même n'a pas été réévalué depuis.

## 3. ACTIVE NEXT STEP

*(Un seul chantier principal autorisé — pas une liste.)*

**Epistemic Dialogue v0.** Knowledge Model v0 est maintenant livré, fusionné et structurellement à tête unique par `(entity_id, subject)` — branchement après une première confirmation fermé par `v25`, course de première confirmation concurrente fermée par `v26` (2026-08-09). La mémoire que le dialogue devra interroger est fermée avant que le dialogue lui-même soit construit — décision explicite (2026-08-09) : le Knowledge Model doit garantir une réponse canonique unique à « Pepperyn connaît-il déjà cette convention ? » avant qu'un mécanisme n'apprenne à s'appuyer dessus. Prochaine étape : fusionner `docs/Architecture/Cognitive/EPISTEMIC_DIALOGUE_V0_IMPLEMENTATION_CONTRACT.md` (branche `architecture/epistemic-dialogue-v0-2026-08-09`), puis implémenter sa plus petite tranche verticale (détecteur FRU-stub → `ClarificationNeed` → `KnowledgeModel.recall()/confirm()` → réponse humaine simulée, zéro LLM, zéro chat réel).

**Exception explicitement autorisée à avancer en parallèle** (justifiée dans `LEGACY_MIGRATION_REVIEW_REPORT.md` Mission 9 et confirmée ici) : correction du Trust Boundary (contournement d'anonymisation du Conversation Engine V2) — aucune dépendance technique avec la séquence ci-dessus, son report prolonge un écart déjà prouvé entre promesse et comportement réel.

## 4. DEFERRED WITH TRIGGERS

*(Renvoi exclusif vers `STRATEGIC_DEFERRED_WORK_REGISTER.md` — pas de duplication de contenu ici, conforme à la règle « un seul registre ».)* Chantiers concernés : relâchement de la cardinalité Entity:Engagement (§1.2.a, déclencheur = premier besoin réel de second mandat), résolution Evidence/DecisionArc après suppression d'Entity (§1.2.b), **sémantique des issues de capture Evidence** (§1.3.a, déclencheur = premier consommateur dont la correction dépend de la présence d'Evidence — pas une autorisation d'implémenter la table d'issues), **provenance Evidence au niveau assertion** (§1.3.b, déclencheur = première fonctionnalité revendiquant une provenance au niveau assertion/recommandation — jusque-là toute UX Evidence reste au niveau contexte d'analyse), **normalisation des formats de mois numériques `temporal_normalizer.py`** (§1.4.a, déclencheur = avant de revendiquer la conformité temporelle réelle du cas Phidani — kernel FTE correct, gap de normalisation encore ouvert), **réconciliation `QuantifiedImpact.is_current_period` vs FTE** (§1.4.b, déclencheur = avant qu'un composant revendique une interprétation canonique unique de « période courante »), architecture des agents IA, Enterprise Familiarization, Exception & Reconciliation, Recommendation Engine, Attention Score, Decision Follow-up, Business History (au-delà du sujet unique `EXPENSE_SIGN_CONVENTION` couvert par Knowledge Model v0, livré — section 1), partage par rôles, connecteurs ERP/API/MCP, choix de modèle IA, évolution des exports, dette de cache/stockage, doublon feedback. Chacun porte son propre déclencheur dans le registre — voir la mise à jour Phase 13.

## 5. VISION / PARKING

*(Renvoi vers `STRATEGIC_DEFERRED_WORK_REGISTER.md` §4 — Decision Simulation Engine, BYOM/local, extension à d'autres professions.)*

---

## Ce que ce Product Board reconstruit corrige, explicitement

Par rapport à la version `main` : réintègre le North Star, la Vision et les règles de priorisation, absents de la version `main` alors que non contredits par le code. Par rapport à la version gouvernance (`dfb8a47`) : retire toute mention de Monthly Review Engine comme capability en cours (obsolète, contredite par le code — Portfolio Intelligence est la capacité réellement livrée) et toute métrique présentant T1/T2 comme validés sur `main` (faux — validés sur un projet de test, jamais fusionnés).

**Une seule capacité ne peut jamais apparaître simultanément en section 1 et en section 4** — test de cohérence structurel : Portfolio Intelligence, T1C-A, T1C-B, T2A, Evidence Ledger Consumer #1, DecisionArc ↔ Engagement, Decision Memory Integrity Repair, FTE v0 (kernel) et Knowledge Model v0 (section 1) n'apparaissent nulle part dans le registre de chantiers différés pour eux-mêmes ; seuls leurs chantiers de suivi (cardinalité Entity:Engagement §1.2.a, résolution Evidence/DecisionArc §1.2.b, sémantique des issues de capture Evidence §1.3.a, provenance Evidence au niveau assertion §1.3.b, interaction FK/trigger entity_id/engagement_id sur arc CLOSED §1.6.c, normalisation `temporal_normalizer.py` §1.4.a, réconciliation `is_current_period` §1.4.b, Familiarization) y apparaissent, chacun avec son propre déclencheur.

---

**PRODUCT_BOARD.md — RÉFÉRENCE CANONIQUE UNIQUE, PROMUE LORS DE LA CONSOLIDATION 2026-08-07.**
